---
phase: 10
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/vercel.json
  - docs/deployment/vercel-deploy.md
  - apps/dashboard/.env.production.example
autonomous: true
requirements:
  - DEPLOY-03
tags:
  - vercel
  - dashboard
  - nextjs
  - signalr
  - wss
must_haves:
  truths:
    - "apps/dashboard/vercel.json configures Vercel for the monorepo subdirectory build (apps/dashboard root)"
    - "apps/dashboard/.env.production.example documents NEXT_PUBLIC_API_URL=https://<DUCKDNS_DOMAIN> as the production value"
    - "docs/deployment/vercel-deploy.md walks through: connect repo → set root directory → set env vars → deploy → record URL"
    - "Vercel runbook explicitly notes NEXT_PUBLIC_API_URL is baked at build time (any change requires redeploy)"
    - "Vercel runbook documents the post-deploy step: copy the Vercel URL into root .env.prod as CORS_ALLOWED_ORIGINS, then run docker compose up -d api on Oracle (handled by Plan 10-05)"
    - "Vercel runbook validates SignalR WSS handshake from the Vercel domain (no mixed content)"
  artifacts:
    - path: "apps/dashboard/vercel.json"
      provides: "Vercel build configuration: framework=nextjs, install/build/output commands matching pnpm workspace, output dir .next"
      contains: "nextjs"
    - path: "apps/dashboard/.env.production.example"
      provides: "Vercel env var template: NEXT_PUBLIC_API_URL=https://<DUCKDNS_DOMAIN>, API_URL=https://<DUCKDNS_DOMAIN>"
      contains: "NEXT_PUBLIC_API_URL=https://"
    - path: "docs/deployment/vercel-deploy.md"
      provides: "Step-by-step deploy runbook: account setup, repo connection, env var configuration, smoke test, CORS handoff"
      contains: "NEXT_PUBLIC_API_URL"
      min_lines: 60
  key_links:
    - from: "apps/dashboard/.env.production.example"
      to: "Vercel Project Settings → Environment Variables"
      via: "operator copies values into Vercel dashboard before triggering build"
      pattern: "NEXT_PUBLIC_API_URL"
    - from: "docs/deployment/vercel-deploy.md (CORS handoff step)"
      to: ".env.prod (root, on Oracle server) CORS_ALLOWED_ORIGINS"
      via: "Vercel deployment URL written into root .env.prod by Plan 10-05"
      pattern: "CORS_ALLOWED_ORIGINS"
---

<objective>
Author the Vercel deployment configuration and runbook for the Next.js dashboard. The
dashboard runs on Vercel free tier (D-04) — it is NOT in `docker-compose.prod.yml`.
The runbook explicitly orders the steps so the Vercel URL is known BEFORE Plan 10-05
sets `CORS_ALLOWED_ORIGINS` on the Oracle API, satisfying the gate constraint that
"Vercel deploy (10-03 → produces URL) runs before final API restart (10-05)".

Key technical risks the runbook addresses:
1. `NEXT_PUBLIC_API_URL` is baked at build time — set it correctly the first time
2. Vercel monorepo builds need `Root Directory = apps/dashboard`
3. SignalR WSS handshake from HTTPS Vercel → HTTPS Oracle must succeed (no mixed
   content); the Nginx config in Plan 10-02 handles the server side

Purpose: Closes DEPLOY-03 (Dashboard deployed to Vercel free tier). Produces the
artifact the operator follows to obtain the Vercel deployment URL that Plan 10-05
will plug into the Oracle API's `CORS_ALLOWED_ORIGINS`.

Output: One vercel.json, one .env.production.example, one runbook.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-production-deployment/10-CONTEXT.md
@.planning/phases/10-production-deployment/10-RESEARCH.md

@apps/dashboard/package.json
@apps/dashboard/next.config.ts
@apps/dashboard/.env.example
@apps/dashboard/Dockerfile
</context>

<interfaces>
<!-- Dashboard build commands (existing) -->
- Root `package.json` uses pnpm workspaces.
- `apps/dashboard/package.json` build script: `next build` (verified standard Next.js).
- Shared types: dashboard depends on `packages/shared-types` (must be built before
  dashboard build). On Vercel this is solved by setting the build command at the
  monorepo root so pnpm/turbo runs the dependency graph correctly.

<!-- DuckDNS domain placeholder -->
- Same `<DUCKDNS_DOMAIN>` placeholder as Plan 10-02. The operator substitutes once at
  deploy time in Plan 10-05 / when setting Vercel env vars.

<!-- WSS requirement (D-05) -->
- Vercel serves the dashboard over HTTPS (`https://<project>.vercel.app`).
- Browser security blocks `ws://` from an `https://` page (mixed content).
- The SignalR client (Phase 9) negotiates the transport URL based on
  `NEXT_PUBLIC_API_URL`. With `https://<DUCKDNS_DOMAIN>` it upgrades to `wss://`
  automatically — provided Nginx `/hubs/` location uses the upgrade headers
  (Plan 10-02).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author Vercel build configuration (vercel.json + env template)</name>
  <files>apps/dashboard/vercel.json, apps/dashboard/.env.production.example</files>
  <read_first>
    - apps/dashboard/package.json (confirm build script and dependencies)
    - apps/dashboard/next.config.ts (confirm no custom output mode that conflicts with Vercel defaults)
    - apps/dashboard/.env.example (template to base .env.production.example on)
    - .planning/phases/10-production-deployment/10-RESEARCH.md (section: "Pattern 6: Vercel Deployment")
    - .planning/phases/10-production-deployment/10-CONTEXT.md (D-04 Vercel only; D-05 NEXT_PUBLIC_API_URL = https://duckdns; SignalR WSS)
  </read_first>
  <action>
    Sub-step 1a — Create `apps/dashboard/vercel.json`:

    Vercel auto-detects Next.js when `Root Directory = apps/dashboard` is set in the
    project settings UI. The vercel.json provides explicit build commands for the
    monorepo case (pnpm workspace requires building shared-types first). The
    `installCommand` runs at the monorepo root so workspace packages are linked.

    ```json
    {
      "$schema": "https://openapi.vercel.sh/vercel.json",
      "framework": "nextjs",
      "buildCommand": "cd ../.. && pnpm install --frozen-lockfile --filter @web-crawler/dashboard... && pnpm --filter @web-crawler/shared-types build && pnpm --filter @web-crawler/dashboard build",
      "installCommand": "echo 'install handled in buildCommand to share workspace context'",
      "outputDirectory": ".next",
      "regions": ["sin1"]
    }
    ```

    Notes on each field:
    - `framework: nextjs` — Vercel enables Next.js optimizations (ISR, image opt, etc.)
    - `buildCommand` — runs from `apps/dashboard` working dir (Vercel root directory).
      `cd ../..` jumps to monorepo root; `pnpm install --filter ...` materializes the
      workspace; then build shared-types, then build dashboard. This is the canonical
      Vercel pnpm-monorepo pattern.
    - `outputDirectory: .next` — Vercel's default for Next.js; explicit for clarity.
    - `regions: ["sin1"]` — Singapore (closest to most Oracle Cloud APAC regions). If the
      Oracle instance is in a different region, the operator updates this. Single region
      is correct for Hobby tier (free).

    Sub-step 1b — Create `apps/dashboard/.env.production.example`:

    ```
    # Vercel production environment variables for the dashboard.
    # Set these in: Vercel Dashboard → <project> → Settings → Environment Variables (Production scope).
    # DO NOT copy this file into a real .env.production — Vercel reads from its UI, not this file.

    # Public — baked into the JS bundle at build time. Browser-side fetch and SignalR connect target.
    # MUST be https:// so SignalR upgrades to wss:// (browser mixed-content rules).
    NEXT_PUBLIC_API_URL=https://<DUCKDNS_DOMAIN>

    # Server-only — used by app/lib server-side fetchers (e.g., lib/api.server.ts).
    # Same value as NEXT_PUBLIC_API_URL; kept separate so server code never reads the public-prefixed var.
    API_URL=https://<DUCKDNS_DOMAIN>

    # NODE_ENV is set automatically by Vercel to "production" for Production deployments.
    ```
  </action>
  <verify>
    <automated>test -f apps/dashboard/vercel.json &amp;&amp; test -f apps/dashboard/.env.production.example &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('apps/dashboard/vercel.json','utf8'))" &amp;&amp; grep -q '"framework": "nextjs"' apps/dashboard/vercel.json &amp;&amp; grep -q '"buildCommand"' apps/dashboard/vercel.json &amp;&amp; grep -q "shared-types build" apps/dashboard/vercel.json &amp;&amp; grep -q "NEXT_PUBLIC_API_URL=https://" apps/dashboard/.env.production.example &amp;&amp; grep -q "API_URL=https://" apps/dashboard/.env.production.example &amp;&amp; ! grep -q "NEXT_PUBLIC_API_URL=http://localhost" apps/dashboard/.env.production.example &amp;&amp; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/dashboard/vercel.json` exists and is valid JSON (`node -e "JSON.parse(...)"` succeeds)
    - vercel.json has `"framework": "nextjs"`
    - vercel.json `buildCommand` includes `pnpm --filter @web-crawler/shared-types build` (workspace dependency built first)
    - vercel.json `buildCommand` includes `pnpm --filter @web-crawler/dashboard build`
    - `apps/dashboard/.env.production.example` exists
    - File contains `NEXT_PUBLIC_API_URL=https://<DUCKDNS_DOMAIN>` (HTTPS, with placeholder)
    - File contains `API_URL=https://<DUCKDNS_DOMAIN>`
    - File does NOT contain any `http://localhost` (would mislead the operator into setting dev URLs on Vercel)
  </acceptance_criteria>
  <done>
    vercel.json is valid JSON with Next.js framework, monorepo-aware buildCommand; .env.production.example documents the two required env vars with HTTPS scheme.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author Vercel deployment runbook with WSS smoke test and CORS handoff step</name>
  <files>docs/deployment/vercel-deploy.md</files>
  <read_first>
    - .planning/phases/10-production-deployment/10-RESEARCH.md (section: "Pattern 6: Vercel Deployment", "Pitfall 6: NEXT_PUBLIC_API_URL Baked at Build Time", "Pitfall 3: CORS_ALLOWED_ORIGINS Value Not Known at Compose Write Time")
    - .planning/phases/10-production-deployment/10-CONTEXT.md (D-04, D-05, D-06)
    - .planning/phases/09-real-time-dashboard-integration/09-CONTEXT.md (SignalR client behavior — included for context only)
  </read_first>
  <action>
    Create `docs/deployment/vercel-deploy.md`:

    ```markdown
    # Vercel Deployment Runbook — Dashboard (Hobby Tier)

    The Next.js dashboard runs on Vercel free Hobby tier (D-04). It is NOT in
    `docker-compose.prod.yml`. Vercel builds the dashboard from this monorepo and serves
    it at `https://<project>.vercel.app`. The dashboard calls the Oracle API over HTTPS
    and connects to SignalR via WSS (D-05).

    ## Prerequisites

    - Plan 10-02 cert bootstrap completed; `https://<DUCKDNS_DOMAIN>/health` returns 200
      from an external machine (so the browser can reach the API once CORS is set)
    - DuckDNS domain known (e.g., `mycrawler.duckdns.org`)
    - GitHub repo for this project pushed and accessible
    - Vercel account (sign up free at https://vercel.com — Hobby tier permits personal/
      non-commercial projects per Vercel ToS)

    ## Step 1 — Connect Repo to Vercel

    1. Sign in to https://vercel.com/new
    2. Click **Import Git Repository** → select this repo
    3. On the project configuration page:
       - **Framework Preset:** Next.js (auto-detected)
       - **Root Directory:** `apps/dashboard` (CRITICAL — without this, build fails because Vercel runs commands from monorepo root, which has no Next.js project)
       - **Build & Output Settings:** leave defaults (the in-repo `apps/dashboard/vercel.json` overrides them)
       - **Install Command, Build Command, Output Directory:** leave at "Override: off" — `vercel.json` provides them

    ## Step 2 — Set Environment Variables (Production scope)

    Before clicking **Deploy**, set these in **Project Settings → Environment Variables**
    (scope: Production):

    | Variable | Value |
    |----------|-------|
    | `NEXT_PUBLIC_API_URL` | `https://<DUCKDNS_DOMAIN>` (e.g., `https://mycrawler.duckdns.org`) |
    | `API_URL`             | `https://<DUCKDNS_DOMAIN>` (same value) |

    **CRITICAL:** `NEXT_PUBLIC_API_URL` is baked into the JS bundle at build time. If you
    set it wrong and deploy, the only fix is to update it here and trigger a **new**
    deployment. Runtime env var changes do not affect already-built bundles for
    `NEXT_PUBLIC_*` variables.

    ## Step 3 — Deploy

    Click **Deploy**. The first build takes ~2-3 minutes (pnpm install + shared-types
    build + Next.js build).

    On success, note the **Production URL**:
    - Default: `https://<your-project-name>.vercel.app` (stable)
    - Also: a unique deploy URL per commit (`https://<project>-<hash>.vercel.app`) — these
      are useful for previews but not the URL the operator pins to CORS

    **Record this URL** — it is the input to Plan 10-05 (CORS_ALLOWED_ORIGINS).

    ## Step 4 — Pre-CORS Smoke Test (expect failure)

    Open `https://<your-project-name>.vercel.app` in a browser. Open DevTools → Network
    tab. You should see:
    - Dashboard HTML loads (Vercel serves it; no API calls yet)
    - First API call (e.g., `/api/entries`) → **fails with CORS error** in console:
      `Access to fetch at 'https://<DUCKDNS_DOMAIN>/api/entries' from origin 'https://<your-project-name>.vercel.app' has been blocked by CORS policy`

    This failure is **expected** — Plan 10-05 fixes it. Do not skip Step 5.

    ## Step 5 — CORS Handoff to Oracle (Plan 10-05 executes this)

    Plan 10-05's runbook tells the operator to:
    1. SSH to the Oracle server
    2. Edit `/opt/webcrawler/.env.prod` and set:
       ```
       CORS_ALLOWED_ORIGINS=https://<your-project-name>.vercel.app
       ```
       (Use the stable production URL from Step 3 — NOT a preview deploy URL.)
    3. Restart only the API container:
       ```
       docker compose -f docker-compose.prod.yml up -d --force-recreate api
       ```

    The api container reads the new env var and updates its CORS policy (Program.cs reads
    `CORS_ALLOWED_ORIGINS` per Plan 10-01).

    ## Step 6 — Post-CORS Validation

    Reload `https://<your-project-name>.vercel.app`. Verify:

    1. **API fetch works** — Network tab shows `200 OK` on `https://<DUCKDNS_DOMAIN>/api/entries`
       with response body containing entries
    2. **SignalR WSS handshake** — Network tab → filter `WS` → look for a request to
       `https://<DUCKDNS_DOMAIN>/hubs/dashboard/negotiate` (returns 200) followed by a
       `wss://<DUCKDNS_DOMAIN>/hubs/dashboard?id=...` connection that shows
       `101 Switching Protocols`
    3. **No mixed-content warnings** — Console tab shows no errors about insecure WS or
       mixed content
    4. **Connection indicator shows "Connected"** in the nav bar (per Phase 9 D-03)

    If SignalR falls back to Long Polling (repeated POST to `/hubs/dashboard/...` every
    few seconds instead of a single WSS), the issue is server-side: re-check the
    nginx.conf `/hubs/` location for `proxy_http_version 1.1`, `Upgrade`, and
    `Connection` headers (Plan 10-02 Pitfall 4).

    ## Troubleshooting

    | Symptom | Likely cause | Fix |
    |---------|--------------|-----|
    | Build fails with "Cannot find module @web-crawler/shared-types" | Root Directory not set to apps/dashboard, OR vercel.json buildCommand missing shared-types build step | Verify Step 1 Root Directory; confirm vercel.json buildCommand from Plan 10-03 Task 1 |
    | Build succeeds but pages 404 | Output Directory misconfigured | Leave Output Directory in Vercel UI as default; vercel.json sets `.next` |
    | CORS errors persist after Plan 10-05 | Vercel URL in CORS_ALLOWED_ORIGINS has trailing slash, OR API container not restarted, OR wrong URL recorded | Re-check Step 5 exact URL; `docker compose -f docker-compose.prod.yml restart api` |
    | SignalR shows "Disconnected" / Long Polling | nginx.conf missing upgrade headers OR cert handshake failed | Verify `curl -sv https://<DUCKDNS_DOMAIN>/health` returns 200; re-check Plan 10-02 nginx.conf |
    | NEXT_PUBLIC_API_URL change ignored | Already-built bundle still has old value | Trigger redeploy: Vercel Dashboard → Deployments → "Redeploy" on latest |

    ## Limits to Watch (Hobby Tier)

    | Limit | Value | Notes |
    |-------|-------|-------|
    | Bandwidth | 100 GB / month | Dashboard is small (< 1MB per pageview); ample headroom |
    | Build executions | Unlimited | Free deploys |
    | Serverless function execution | 100 GB-hours / month | This app uses minimal server functions; mostly static + client fetch |
    | Concurrent builds | 1 | Only one deploy at a time — second push queues |

    See https://vercel.com/docs/plans/hobby for current limits.
    ```
  </action>
  <verify>
    <automated>test -f docs/deployment/vercel-deploy.md &amp;&amp; grep -q "Root Directory" docs/deployment/vercel-deploy.md &amp;&amp; grep -q "apps/dashboard" docs/deployment/vercel-deploy.md &amp;&amp; grep -q "NEXT_PUBLIC_API_URL" docs/deployment/vercel-deploy.md &amp;&amp; grep -q "baked.*build time\|build time.*baked\|baked into the JS bundle" docs/deployment/vercel-deploy.md &amp;&amp; grep -q "CORS_ALLOWED_ORIGINS" docs/deployment/vercel-deploy.md &amp;&amp; grep -q "101 Switching Protocols\|wss://" docs/deployment/vercel-deploy.md &amp;&amp; grep -q "Plan 10-05" docs/deployment/vercel-deploy.md &amp;&amp; wc -l docs/deployment/vercel-deploy.md | awk '{exit ($1 < 60) ? 1 : 0}' &amp;&amp; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/deployment/vercel-deploy.md` exists with at least 60 lines
    - Contains the literal phrase "Root Directory" and references `apps/dashboard`
    - Contains `NEXT_PUBLIC_API_URL` with explanation that it is baked at build time
    - Contains `CORS_ALLOWED_ORIGINS` with explicit handoff to Plan 10-05
    - Contains `wss://` (or `101 Switching Protocols`) in the SignalR validation step
    - Has a troubleshooting table covering at least: build-time CORS, Long Polling fallback, NEXT_PUBLIC env var change
    - Step 5 explicitly orders: edit .env.prod → restart api container (matches gate constraint)
  </acceptance_criteria>
  <done>
    Runbook is comprehensive: connect repo, set env vars, deploy, smoke test (pre-CORS expected fail), CORS handoff to Plan 10-05, post-CORS validation including WSS upgrade. Operator can follow steps in order without inferring missing context.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (https://vercel.app) → Oracle API (https://duckdns) | Cross-origin — gated by CORS_ALLOWED_ORIGINS allowlist |
| Vercel build environment → GitHub repo | Read-only via OAuth; build commands run from vercel.json |
| Vercel CDN → end-user browser | TLS termination at Vercel edge; certs managed by Vercel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-12 | E (Elevation of Privilege) | Wildcard CORS allowing any origin with credentials | mitigate | `CORS_ALLOWED_ORIGINS` is set to the EXACT Vercel URL (not `*`); Plan 10-01 preserves `.AllowCredentials()` (Phase 6 D-07). Plan 10-05 step explicitly says "no trailing slash, single exact origin". |
| T-10-13 | I (Information Disclosure) | NEXT_PUBLIC_API_URL set to dev/staging URL exposes internal endpoints | accept | Runbook explicitly instructs setting to `https://<DUCKDNS_DOMAIN>`; .env.production.example contains placeholder, not real value. Operator must confirm. |
| T-10-14 | T (Tampering) | Mixed content: ws:// from https:// page | mitigate | NEXT_PUBLIC_API_URL is HTTPS; SignalR client auto-negotiates `wss://`; browser blocks any `ws://` fallback. Runbook smoke test verifies `101 Switching Protocols` in DevTools. |
| T-10-15 | S (Spoofing) | Preview deployment URL used in CORS allowlist instead of production URL | mitigate | Runbook Step 3 explicitly says "stable production URL — NOT a preview deploy URL"; Step 5 in CORS handoff says "use stable URL from Step 3". |
| T-10-16 | D (Denial of Service) | Vercel free tier bandwidth/build limits exhausted | accept | Documented in "Limits to Watch" section; personal-use traffic profile fits inside Hobby limits with large headroom. |
</threat_model>

<verification>
- vercel.json is valid JSON; framework=nextjs; monorepo-aware buildCommand
- .env.production.example documents HTTPS production URL (no localhost)
- Runbook documents Root Directory = apps/dashboard
- Runbook explains NEXT_PUBLIC_API_URL build-time baking
- Runbook orders CORS handoff: deploy Vercel → record URL → Plan 10-05 sets it on Oracle → restart api
</verification>

<success_criteria>
- DEPLOY-03 (Dashboard deployed to Vercel free tier): satisfied by vercel.json + runbook
- D-04 / D-05 / D-06 honored
- Plan 10-05 has clear input artifact: the Vercel production URL
- Operator can deploy without trial-and-error on monorepo settings
</success_criteria>

<output>
After completion, create `.planning/phases/10-production-deployment/10-03-SUMMARY.md` documenting:
- apps/dashboard/vercel.json path + framework + buildCommand
- apps/dashboard/.env.production.example path + env vars documented
- docs/deployment/vercel-deploy.md path + 6-step deploy flow
- Open follow-ups: Plan 10-05 receives the Vercel URL as input to CORS_ALLOWED_ORIGINS
</output>
