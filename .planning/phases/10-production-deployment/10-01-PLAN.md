---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.prod.yml
  - apps/api/Program.cs
  - apps/api/.env.prod.example
  - apps/crawler/.env.prod.example
  - .env.prod.example
  - .gitignore
autonomous: true
requirements:
  - DEPLOY-01
  - INFRA-02
tags:
  - docker
  - compose
  - infrastructure
  - production
  - arm64
must_haves:
  truths:
    - "docker-compose.prod.yml is a standalone file (no extends) defining postgres, redis, crawler, api, nginx services — no dashboard service"
    - "Every prod service has platform: linux/arm64, restart: always, and deploy.resources.limits for Ampere A1 (4 vCPU / 24GB)"
    - "Redis command line is exactly: redis-server --maxmemory-policy noeviction --appendonly yes --appendfsync everysec"
    - "Named volume redis_data is mounted at /data on the redis service AND declared in top-level volumes: block"
    - "Postgres and Redis do NOT expose host ports in prod compose (no ports: 5432/6379) — only nginx exposes 80/443"
    - "apps/api/Program.cs reads CORS_ALLOWED_ORIGINS env var (renamed from CORS_ORIGINS) per D-06"
    - "docker compose -f docker-compose.prod.yml config exits 0 (valid YAML, resolvable env vars)"
  artifacts:
    - path: "docker-compose.prod.yml"
      provides: "Standalone production compose with 5 services (no dashboard), ARM64, restart:always, resource limits, redis AOF, no exposed DB ports"
      contains: "appendonly yes"
      min_lines: 100
    - path: "apps/api/Program.cs"
      provides: "CORS origin env var renamed to CORS_ALLOWED_ORIGINS"
      contains: "CORS_ALLOWED_ORIGINS"
    - path: "apps/api/.env.prod.example"
      provides: "Production API env template with ASPNETCORE_ENVIRONMENT=Production, prod DB connection, CORS_ALLOWED_ORIGINS placeholder"
      contains: "ASPNETCORE_ENVIRONMENT=Production"
    - path: "apps/crawler/.env.prod.example"
      provides: "Production crawler env template with NODE_ENV=production"
      contains: "NODE_ENV=production"
    - path: ".env.prod.example"
      provides: "Root env template documenting POSTGRES_PASSWORD, CORS_ALLOWED_ORIGINS, TELEGRAM/DISCORD secrets used by docker-compose.prod.yml"
      contains: "CORS_ALLOWED_ORIGINS"
    - path: ".gitignore"
      provides: ".env.prod and apps/*/.env.prod are gitignored (secrets not committed)"
      contains: ".env.prod"
  key_links:
    - from: "docker-compose.prod.yml"
      to: "apps/api/Program.cs"
      via: "CORS_ALLOWED_ORIGINS env var passed into api service environment"
      pattern: "CORS_ALLOWED_ORIGINS"
    - from: "docker-compose.prod.yml redis service"
      to: "redis_data named volume"
      via: "redis_data:/data mount + top-level volumes: redis_data"
      pattern: "redis_data:/data"
    - from: "docker-compose.prod.yml"
      to: "apps/*/Dockerfile"
      via: "build.context: ./apps/<svc> build.dockerfile: Dockerfile (matches existing relative COPY paths)"
      pattern: "context: ./apps/"
---

<objective>
Author `docker-compose.prod.yml` as a standalone production manifest containing postgres,
redis, crawler, api, and nginx services (dashboard runs on Vercel — D-04). Apply all
D-08 overrides (restart:always, resource limits for Ampere A1, ARM64 platform tag, no
source bind-mounts, no exposed DB/Redis ports, redis_data named volume). Enable Redis
AOF persistence (D-07) via the exact command string. Rename the CORS origin env var in
`apps/api/Program.cs` from `CORS_ORIGINS` to `CORS_ALLOWED_ORIGINS` (D-06) so the API
reads the same name documented across this phase and the Vercel deploy plan. Create
prod `.env.prod.example` templates and gitignore their `.env.prod` counterparts.

Purpose: Closes DEPLOY-01 (prod compose exists, all overrides applied) and INFRA-02
(every service tagged `platform: linux/arm64`). This is the foundation Wave 1 task —
every later plan reads from this compose file or runs commands against it.

Output: One production compose file, one Program.cs edit, three `.env.prod.example`
templates, one `.gitignore` update.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-production-deployment/10-CONTEXT.md
@.planning/phases/10-production-deployment/10-RESEARCH.md

@docker-compose.yml
@apps/api/Program.cs
@apps/api/Dockerfile
@apps/crawler/Dockerfile
@apps/api/.env.example
@apps/crawler/.env.example
@.env.example
@.gitignore
</context>

<interfaces>
<!-- Existing CORS pattern in Program.cs that must be renamed -->
From apps/api/Program.cs (lines 74-83) — CURRENT state:
```csharp
builder.Services.AddCors(options =>
{
    options.AddDefaultPolicy(policy =>
        policy.WithOrigins(
                Environment.GetEnvironmentVariable("CORS_ORIGINS")?.Split(',')
                    ?? new[] { "http://localhost:3000" })
              .AllowAnyHeader()
              .AllowAnyMethod()
              .AllowCredentials());
});
```
This plan renames `CORS_ORIGINS` → `CORS_ALLOWED_ORIGINS` (per D-06). The default
fallback `http://localhost:3000` is preserved for local dev.

<!-- Existing Dockerfile build contexts (do NOT change) -->
- apps/api/Dockerfile uses `COPY apps/api/*.csproj ./` — assumes context `.` (monorepo root). Verify and align.
- apps/crawler/Dockerfile uses `COPY package.json pnpm-workspace.yaml ...` — assumes context `.` (monorepo root).
- Dev docker-compose.yml currently uses `context: ./apps/crawler` which is a known mismatch — prod compose MUST use `context: .` and `dockerfile: apps/crawler/Dockerfile` for both crawler and api.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author docker-compose.prod.yml (standalone, ARM64, AOF, no dashboard)</name>
  <files>docker-compose.prod.yml</files>
  <read_first>
    - docker-compose.yml (current dev compose — copy structure for postgres/redis/crawler/api; do not include dashboard)
    - apps/api/Dockerfile (confirm COPY paths assume monorepo-root context)
    - apps/crawler/Dockerfile (confirm COPY paths assume monorepo-root context)
    - apps/api/Program.cs (confirm API listens on port 5000 internally — already set via ASPNETCORE_URLS=http://+:5000)
    - .planning/phases/10-production-deployment/10-RESEARCH.md (sections: "docker-compose.prod.yml Skeleton", "Pattern 3: Redis AOF Persistence Command", "Pattern 4: Resource Limits for Ampere A1")
    - .planning/phases/10-production-deployment/10-CONTEXT.md (D-04 no dashboard, D-07 redis command, D-08 prod overrides)
  </read_first>
  <action>
    Create a new file `docker-compose.prod.yml` at repo root. Do NOT include a top-level `version:` key (obsolete per Compose v2+). Use the exact YAML below — this is a complete standalone manifest (no `extends`, no merging with docker-compose.yml).

    ```yaml
    # Production compose for Oracle Cloud Ampere A1 (ARM64, 4 vCPU, 24GB RAM).
    # Standalone — does NOT extend docker-compose.yml.
    # Deploy: SSH to Oracle server → git pull → docker compose -f docker-compose.prod.yml build → up -d
    services:
      postgres:
        image: postgres:16-alpine
        platform: linux/arm64
        restart: always
        environment:
          POSTGRES_DB: webcrawler
          POSTGRES_USER: crawler
          POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
        volumes:
          - postgres_data:/var/lib/postgresql/data
        healthcheck:
          test: ["CMD", "pg_isready", "-U", "crawler", "-d", "webcrawler"]
          interval: 5s
          timeout: 5s
          retries: 5
          start_period: 10s
        deploy:
          resources:
            limits:
              cpus: '1.0'
              memory: 2G
            reservations:
              cpus: '0.25'
              memory: 256M

      redis:
        image: redis:7-alpine
        platform: linux/arm64
        restart: always
        # D-07: AOF persistence + noeviction (required by BullMQ).
        # everysec = fsync once per second (durability/performance balance).
        command: redis-server --maxmemory-policy noeviction --appendonly yes --appendfsync everysec
        volumes:
          - redis_data:/data
        healthcheck:
          test: ["CMD", "redis-cli", "ping"]
          interval: 5s
          timeout: 5s
          retries: 3
          start_period: 5s
        deploy:
          resources:
            limits:
              cpus: '0.5'
              memory: 512M
            reservations:
              cpus: '0.1'
              memory: 128M

      crawler:
        build:
          context: .
          dockerfile: apps/crawler/Dockerfile
        platform: linux/arm64
        restart: always
        env_file:
          - ./apps/crawler/.env.prod
        environment:
          NODE_ENV: production
          RIOT_API_KEY: ${RIOT_API_KEY:-}
        depends_on:
          postgres:
            condition: service_healthy
          redis:
            condition: service_healthy
        healthcheck:
          test: ["CMD", "node", "-e", "process.exit(0)"]
          interval: 30s
          timeout: 10s
          retries: 3
          start_period: 15s
        deploy:
          resources:
            limits:
              cpus: '2.0'
              memory: 4G
            reservations:
              cpus: '0.5'
              memory: 512M

      api:
        build:
          context: .
          dockerfile: apps/api/Dockerfile
        platform: linux/arm64
        restart: always
        env_file:
          - ./apps/api/.env.prod
        environment:
          ASPNETCORE_ENVIRONMENT: Production
          ASPNETCORE_URLS: http://+:5000
          CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
          TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
          TELEGRAM_CHAT_ID: ${TELEGRAM_CHAT_ID:-}
          DISCORD_WEBHOOK_URL: ${DISCORD_WEBHOOK_URL:-}
        depends_on:
          postgres:
            condition: service_healthy
          redis:
            condition: service_healthy
        healthcheck:
          test: ["CMD", "sh", "-c", "wget -q -O - http://localhost:5000/health || exit 1"]
          interval: 30s
          timeout: 10s
          retries: 3
          start_period: 30s
        deploy:
          resources:
            limits:
              cpus: '1.0'
              memory: 1G
            reservations:
              cpus: '0.25'
              memory: 256M

      nginx:
        image: nginx:1.27-alpine
        platform: linux/arm64
        restart: always
        ports:
          - "80:80"
          - "443:443"
        volumes:
          - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
          - letsencrypt:/etc/letsencrypt:ro
        depends_on:
          api:
            condition: service_healthy
        healthcheck:
          test: ["CMD", "wget", "-q", "--spider", "http://localhost/health"]
          interval: 30s
          timeout: 10s
          retries: 3
          start_period: 10s
        deploy:
          resources:
            limits:
              cpus: '0.5'
              memory: 256M
            reservations:
              cpus: '0.1'
              memory: 64M

    volumes:
      postgres_data:
      redis_data:
      letsencrypt:
        external: true
    ```

    Notes:
    - `letsencrypt` is `external: true` — Plan 10-02 creates this volume via one-shot Certbot before first `docker compose up`. If the volume does not exist, `docker compose up` will fail with a clear error pointing to Plan 10-02.
    - `./nginx/nginx.conf` is mounted read-only — Plan 10-02 creates that file.
    - No `ports:` on postgres or redis (closed to host network — only nginx exposes 80/443).
    - No `dashboard` service (D-04: dashboard runs on Vercel).
    - Build contexts use `.` (monorepo root) + `dockerfile: apps/<svc>/Dockerfile` to match the existing Dockerfile COPY paths.
    - Crawler Dockerfile internal healthcheck `["CMD","node","-e","process.exit(0)"]` is kept (matches dev pattern — basic liveness only).
  </action>
  <verify>
    <automated>test -f docker-compose.prod.yml &amp;&amp; grep -q "appendonly yes" docker-compose.prod.yml &amp;&amp; grep -q "redis_data:/data" docker-compose.prod.yml &amp;&amp; grep -q "platform: linux/arm64" docker-compose.prod.yml &amp;&amp; ! grep -q "5432:5432" docker-compose.prod.yml &amp;&amp; ! grep -q "6379:6379" docker-compose.prod.yml &amp;&amp; ! grep -q "dashboard:" docker-compose.prod.yml &amp;&amp; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `docker-compose.prod.yml` exists at repo root
    - `grep -c "platform: linux/arm64" docker-compose.prod.yml` returns 5 (one per service)
    - `grep -c "restart: always" docker-compose.prod.yml` returns 5
    - Exact line present: `command: redis-server --maxmemory-policy noeviction --appendonly yes --appendfsync everysec`
    - `redis_data:/data` volume mount present on redis service AND `redis_data:` declared in top-level `volumes:`
    - `letsencrypt:` declared with `external: true` in top-level volumes
    - No `ports:` block under postgres or redis services
    - No `dashboard:` service block exists
    - No top-level `version:` key
    - `docker compose -f docker-compose.prod.yml config --no-interpolate` exits 0 (YAML is valid; missing env vars are OK with --no-interpolate)
  </acceptance_criteria>
  <done>
    Production compose file exists; YAML parses; all 8 grep-verifiable patterns present; postgres/redis ports NOT exposed; dashboard NOT in compose.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename CORS env var in apps/api/Program.cs and create .env.prod.example templates</name>
  <files>apps/api/Program.cs, apps/api/.env.prod.example, apps/crawler/.env.prod.example, .env.prod.example, .gitignore</files>
  <read_first>
    - apps/api/Program.cs (lines 70-90 — the existing CORS_ORIGINS reference)
    - apps/api/.env.example (template to base .env.prod.example on)
    - apps/crawler/.env.example (template to base .env.prod.example on)
    - .env.example (template to base root .env.prod.example on)
    - .gitignore (verify which patterns already exist before adding)
    - .planning/phases/10-production-deployment/10-CONTEXT.md (D-06 locked decision: `CORS_ALLOWED_ORIGINS`, not `CORS_ORIGINS`)
    - .planning/phases/06-signalr-real-time-layer/06-CONTEXT.md (D-07: AllowCredentials() requires explicit origins — preserve the existing AllowCredentials call)
  </read_first>
  <action>
    Sub-step 2a — Rename CORS env var in `apps/api/Program.cs`:
    Replace the single line (~line 78):
    ```csharp
    Environment.GetEnvironmentVariable("CORS_ORIGINS")?.Split(',')
    ```
    with:
    ```csharp
    Environment.GetEnvironmentVariable("CORS_ALLOWED_ORIGINS")?.Split(',')
    ```
    Do NOT touch any other line in Program.cs. Preserve `.AllowAnyHeader()`, `.AllowAnyMethod()`, `.AllowCredentials()` and the localhost fallback array.

    Sub-step 2b — Create `apps/api/.env.prod.example`:
    ```
    # Production API environment variables.
    # Copy to apps/api/.env.prod on the Oracle server. NEVER commit the .env.prod file.
    ASPNETCORE_ENVIRONMENT=Production
    ASPNETCORE_URLS=http://+:5000
    ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=webcrawler;Username=crawler;Password=CHANGE_ME_STRONG_PASSWORD
    REDIS_URL=redis://redis:6379
    # CORS_ALLOWED_ORIGINS is provided via docker-compose.prod.yml environment block (read from .env.prod or shell env).
    # Example value (set in root .env.prod, not here): https://your-project.vercel.app
    ```

    Sub-step 2c — Create `apps/crawler/.env.prod.example`:
    ```
    # Production crawler environment variables.
    # Copy to apps/crawler/.env.prod on the Oracle server. NEVER commit the .env.prod file.
    NODE_ENV=production
    LOG_LEVEL=info
    REDIS_URL=redis://redis:6379
    DATABASE_URL=postgresql://crawler:CHANGE_ME_STRONG_PASSWORD@postgres:5432/webcrawler
    FOOTBALL_DATA_API_KEY=your_token_here
    ```

    Sub-step 2d — Create root `.env.prod.example`:
    ```
    # Root .env.prod — used by docker-compose.prod.yml (variable substitution via ${VAR}).
    # Copy to .env.prod on the Oracle server. NEVER commit the .env.prod file.

    # Postgres credentials (also referenced by apps/api/.env.prod ConnectionStrings).
    POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

    # CORS — set this to the Vercel deployment URL after Plan 10-03 deploys the dashboard.
    # Example: https://your-project.vercel.app  (no trailing slash; comma-separated for multiple).
    CORS_ALLOWED_ORIGINS=https://CHANGE_ME.vercel.app

    # Notification secrets — populate from Telegram / Discord dashboards.
    TELEGRAM_BOT_TOKEN=
    TELEGRAM_CHAT_ID=
    DISCORD_WEBHOOK_URL=

    # Optional API keys.
    RIOT_API_KEY=
    ```

    Sub-step 2e — Update `.gitignore` (append at end if patterns missing):
    Read the file first. Then append (only if not already present) — each as a single line:
    ```
    # Production env files — never commit
    .env.prod
    apps/api/.env.prod
    apps/crawler/.env.prod
    apps/dashboard/.env.prod
    ```
    Use the Edit tool. Do NOT add duplicate lines.
  </action>
  <verify>
    <automated>grep -q "CORS_ALLOWED_ORIGINS" apps/api/Program.cs &amp;&amp; ! grep -q '"CORS_ORIGINS"' apps/api/Program.cs &amp;&amp; test -f apps/api/.env.prod.example &amp;&amp; test -f apps/crawler/.env.prod.example &amp;&amp; test -f .env.prod.example &amp;&amp; grep -q "ASPNETCORE_ENVIRONMENT=Production" apps/api/.env.prod.example &amp;&amp; grep -q "NODE_ENV=production" apps/crawler/.env.prod.example &amp;&amp; grep -q "CORS_ALLOWED_ORIGINS" .env.prod.example &amp;&amp; grep -q "^\.env\.prod$" .gitignore &amp;&amp; (cd apps/api &amp;&amp; dotnet build --no-restore 2>&amp;1 | tail -5 | grep -E "(Build succeeded|0 Error)") &amp;&amp; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "CORS_ALLOWED_ORIGINS" apps/api/Program.cs` returns at least one match
    - `grep -n '"CORS_ORIGINS"' apps/api/Program.cs` returns NO matches (the old name is fully replaced)
    - `apps/api/.env.prod.example` exists; contains `ASPNETCORE_ENVIRONMENT=Production`
    - `apps/crawler/.env.prod.example` exists; contains `NODE_ENV=production`
    - `.env.prod.example` exists at repo root; contains `POSTGRES_PASSWORD=`, `CORS_ALLOWED_ORIGINS=`, `TELEGRAM_BOT_TOKEN=`, `DISCORD_WEBHOOK_URL=`
    - `.gitignore` contains the literal lines `.env.prod`, `apps/api/.env.prod`, `apps/crawler/.env.prod`, `apps/dashboard/.env.prod`
    - `dotnet build apps/api/WebCrawlerApi.csproj` succeeds (proves Program.cs still compiles — single env var rename only)
  </acceptance_criteria>
  <done>
    Program.cs reads `CORS_ALLOWED_ORIGINS`; three `.env.prod.example` files exist; `.gitignore` blocks prod env files from being committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → nginx (80/443) | All external traffic enters here; only TLS-terminated traffic crosses |
| nginx → api (5000) | Internal Docker network; not exposed to host or internet |
| api/crawler → postgres (5432) | Internal Docker network; not exposed |
| api/crawler → redis (6379) | Internal Docker network; not exposed |
| .env.prod files → git | Secrets must never cross this boundary (.gitignore enforces) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | I (Information Disclosure) | postgres / redis ports exposed to host | mitigate | Remove `ports:` blocks from postgres and redis in docker-compose.prod.yml — only nginx 80/443 exposed. Acceptance criterion enforces via `! grep -q "5432:5432"` and `! grep -q "6379:6379"`. |
| T-10-02 | I (Information Disclosure) | .env.prod secrets committed to git | mitigate | `.gitignore` blocks `.env.prod` and `apps/*/.env.prod`; only `.env.prod.example` with placeholder values is tracked. Acceptance criterion verifies via grep on .gitignore. |
| T-10-03 | E (Elevation of Privilege) | CORS misconfiguration allows arbitrary origin with credentials | mitigate | `CORS_ALLOWED_ORIGINS` is explicit env var read by Program.cs (renamed from CORS_ORIGINS); no wildcard accepted; AllowCredentials retained. Plan 10-03 sets the value to the exact Vercel URL. |
| T-10-04 | D (Denial of Service) | Single container OOM starves entire host | mitigate | `deploy.resources.limits` caps crawler at 4G, api at 1G, postgres at 2G, redis at 512M, nginx at 256M. Total ~7.75G of 24G — safe with reservations summing to ~1.2 vCPU / 1.2G. |
| T-10-05 | T (Tampering) | Stale dev image with mounted source code in prod | accept | Prod compose has no `volumes: - .:/app` bind-mounts; images are built from immutable source at deploy time (D-03: `git pull && docker compose build`). |
</threat_model>

<verification>
- `docker compose -f docker-compose.prod.yml config --no-interpolate` exits 0
- All five services declare `platform: linux/arm64`
- Redis command line matches D-07 exactly
- `apps/api` builds successfully after CORS env var rename
- `.env.prod*` files cannot be accidentally committed (gitignore enforces)
</verification>

<success_criteria>
- DEPLOY-01: production compose file authored with all D-08 overrides and no dashboard
- INFRA-02: every service tagged `platform: linux/arm64`
- D-07 satisfied (Redis AOF command exact)
- D-06 satisfied (Program.cs reads CORS_ALLOWED_ORIGINS, ready for Plan 10-03 to supply value)
- All later plans can run `docker compose -f docker-compose.prod.yml <cmd>` without YAML errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-production-deployment/10-01-SUMMARY.md` documenting:
- File created: docker-compose.prod.yml (lines, services, key overrides applied)
- Program.cs CORS env var rename diff
- Three .env.prod.example templates created
- .gitignore updated
- Open follow-ups: letsencrypt external volume must be created by Plan 10-02; nginx.conf must be created by Plan 10-02
</output>
