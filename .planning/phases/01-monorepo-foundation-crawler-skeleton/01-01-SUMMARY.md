---
phase: "01"
plan: "01-01"
subsystem: monorepo-scaffold
tags: [turborepo, pnpm, typescript, nextjs, dotnet, scaffold]
dependency_graph:
  requires: []
  provides: [monorepo-root-config, workspace-structure, shared-types-package, dashboard-scaffold, crawler-scaffold, api-stub]
  affects: [all-subsequent-plans]
tech_stack:
  added: [turbo@2.9.4, typescript@6.0.2, next@16.2.2, react@19, pnpm@10.33.0]
  patterns: [turborepo-tasks, pnpm-workspaces, nextjs-app-router, dotnet8-minimal-api]
key_files:
  created:
    - package.json
    - pnpm-workspace.yaml
    - turbo.json
    - tsconfig.base.json
    - .nvmrc
    - .editorconfig
    - .gitignore
    - apps/crawler/package.json
    - apps/crawler/tsconfig.json
    - apps/crawler/src/index.ts
    - apps/api/package.json
    - apps/api/Program.cs
    - apps/api/WebCrawlerApi.csproj
    - apps/dashboard/package.json
    - apps/dashboard/tsconfig.json
    - apps/dashboard/next.config.ts
    - apps/dashboard/app/layout.tsx
    - apps/dashboard/app/page.tsx
    - apps/dashboard/app/globals.css
    - packages/shared-types/package.json
    - packages/shared-types/tsconfig.json
    - packages/shared-types/src/index.ts
    - packages/shared-types/src/footballData.ts
    - pnpm-lock.yaml
  modified: []
decisions:
  - "Turborepo 2.x tasks key (not pipeline) with schema https://turborepo.dev/schema.json"
  - "tsconfig.base.json uses Node16 module resolution for ESM compatibility"
  - "apps/dashboard tsconfig extends next/typescript (not tsconfig.base.json directly)"
  - "apps/api package.json wraps dotnet build for Turborepo integration"
metrics:
  duration_seconds: 242
  completed_date: "2026-04-07T08:19:37Z"
  tasks_completed: 3
  files_created: 24
  files_modified: 0
---

# Phase 1 Plan 01-01: Turborepo + pnpm Workspace Scaffold Summary

**One-liner:** Turborepo 2.x monorepo with pnpm workspaces, four workspace packages (crawler/api/dashboard/shared-types), Next.js 16 App Router scaffold, and EplStandingsResponse type definitions.

## What Was Built

A complete monorepo skeleton with:

- Root config: `package.json` (packageManager pnpm@10.33.0), `pnpm-workspace.yaml` (apps/* + packages/*), `turbo.json` (tasks-based pipeline for build/dev/lint/type-check), `tsconfig.base.json` (strict ES2022 + Node16 module resolution)
- Developer tooling: `.nvmrc` (node 20), `.editorconfig` (LF line endings, 2-space indent for TS/JSON/YAML), `.gitignore` (with `apps/**/.env` pattern for T-01-01 threat mitigation)
- `apps/crawler`: TypeScript ESM package with tsx dev runner stub
- `apps/api`: .NET 8 minimal API stub with Turborepo-compatible package.json wrapping dotnet build
- `apps/dashboard`: Next.js 16.2.2 App Router with `app/layout.tsx`, `app/page.tsx`, `app/globals.css`
- `packages/shared-types`: Shared TypeScript library exporting `EplStandingsResponse` interface matching football-data.org API shape
- `pnpm-lock.yaml`: Generated by `pnpm install` (34 packages, including turbo, typescript, next, react)

## Verification Results

```
$ cat turbo.json | grep '"tasks"'
  "tasks": {

$ ls apps/
api  crawler  dashboard

$ ls packages/
shared-types

$ pnpm install
Done in 1m 6.3s using pnpm v10.33.0
```

`pnpm install` resolved 96 packages, added 34 new. Warnings about `esbuild` and `sharp` build scripts are expected (pnpm security default — not errors).

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

- `apps/crawler/src/index.ts`: Console.log placeholder only — workers/schedulers registered in later plans (intentional per plan spec)
- `apps/api/Program.cs`: Minimal `/health` endpoint stub — full Serilog wiring in Plan 01-03 (intentional per plan spec)
- `apps/dashboard/app/page.tsx`: Static placeholder page — data views in Phase 7 (intentional per plan spec)

These stubs do not prevent Plan 01-01's goal (monorepo scaffold with `turbo build` capability) from being achieved.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: information-disclosure | .gitignore | `apps/**/.env` pattern added per T-01-01 mitigation — secrets excluded from commits |

## Self-Check: PASSED

All 24 created files confirmed present on disk. Commit `18effae` verified in git log.
