---
context: phase
phase: 07-next-js-dashboard-core-views
activity: UAT
current_test: 1
total_tests: 13
status: paused_awaiting_user
last_updated: 2026-05-19T00:00:00Z
---

# Resume: Phase 7 UAT — Conversational Testing in Progress

## What's Happening

Running `/gsd:verify-work 7` — conversational UAT for Phase 7 (Next.js Dashboard — Core Views).
Paused at **Test 1 of 13** waiting for user to run the dashboard and report results.

UAT file: `.planning/phases/07-next-js-dashboard-core-views/07-UAT.md`
All 13 tests are currently `[pending]`.

## Why This Matters

Phase 7 is the **only phase without a VERIFICATION.md**. The ROADMAP still shows `[ ]`.
Phases 8–10 were built on top of Phase 7, so the code is wired and used — it just was
never formally signed off. Completing this UAT closes the last planning gap before
the project is fully verified at code + human level.

## Current Test (where to resume)

**Test 1: Root Redirect**

Start the dashboard and open `http://localhost:3000`.

> Expected: Browser immediately redirects to `/entries` without any click. Entries page
> content (or loading skeleton) appears in the main area.

Ask the user: "What happened? (pass / fail / skip — or describe what you saw)"

## All 13 Tests (in order)

1. **Root Redirect** — `http://localhost:3000` → redirects to `/entries` ← *current*
2. **Dashboard Layout (Desktop)** — sidebar 240px, nav links, active link highlighted
3. **Mobile Navigation** — hamburger, slide-out drawer, link closes drawer
4. **Entries Page Loads** — data table with category badge, key, payload, timestamp; error state if API down
5. **Category Filter** — dropdown filters table, URL updates `?category=...`
6. **Source Filter** — dropdown filters by source, URL updates `?sourceId=...`
7. **Date Range Filter** — from/to dates filter entries, URL updates
8. **Load More Pagination** — "Load more" appends rows, disappears when exhausted
9. **Sources Page Loads** — table with name/URL/status/crawler type; empty state CTA
10. **Add Source** — modal opens, form submits, new source appears in table
11. **Edit Source** — modal pre-filled, immutable fields disabled, update works
12. **Delete Source** — optimistic delete (row vanishes immediately), rollback on error
13. **Sources Empty State** — empty state UI with "Add Source" button when no sources

## How to Run the Dashboard

```bash
# With Docker:
docker compose up dashboard

# Or dev mode:
pnpm --filter @web-crawler/dashboard dev
# Dashboard at http://localhost:3000
# Requires the .NET API running for real data (docker compose up api postgres redis)
```

## What to Do After UAT

Once all 13 tests are recorded (pass/fail/skip):
- If all pass: Run `/gsd:verify-work` to finalize — the verifier will produce `07-VERIFICATION.md` and mark Phase 7 complete in ROADMAP
- If failures found: Diagnose, fix, and re-test affected cases

## Phase 10 Outstanding Items (separate from this UAT)

The Phase 10 handoff at `.planning/phases/10-production-deployment/.continue-here.md` has two
pre-deploy bug fixes still outstanding:
- **CR-01**: rename env key in `apps/api/.env.prod.example` (line 5) from `ConnectionStrings__DefaultConnection` to `DATABASE_URL`
- **CR-02**: wrap `saveBloomFilter()` in try/catch in `apps/crawler/src/index.ts` (~line 95)

These must be fixed before deploying to Oracle Cloud.

## Next Action

Resume with: `/gsd:verify-work 7`
The UAT session will pick up at Test 1.
