---
phase: 13
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/app/globals.css
  - apps/dashboard/components/layout/Sidebar.tsx
  - apps/dashboard/components/layout/MobileNav.tsx
  - apps/dashboard/components/layout/NavLinks.tsx
  - apps/dashboard/components/layout/PageHeader.tsx
  - apps/dashboard/components/entries/HeroSection.tsx
  - apps/dashboard/components/entries/CategoryFilterTiles.tsx
  - apps/dashboard/components/search/HeroSearchInput.tsx
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-02
user_setup: []
must_haves:
  truths:
    - "Sidebar background renders the warm-dark color #252017 (slightly lighter than previous #1c1814) on both desktop Sidebar and mobile MobileNav header + drawer"
    - "No source file under apps/dashboard/ references the hex literal bg-[#1c1814] or text-[#d8553a] or bg-[#d8553a] — all replaced by bg-sidebar / text-primary / bg-primary"
    - "Active nav item background renders coral (#d8553a) via bg-primary (not the hardcoded hex)"
    - "PageHeader, HeroSection, HeroSearchInput use semantic tokens (text-foreground, text-muted-foreground, border-border, ring-primary) — no zinc-200/zinc-500/zinc-900 hardcodes remain in those files"
    - "Dashboard still builds with zero TypeScript errors after token swap"
  artifacts:
    - path: "apps/dashboard/app/globals.css"
      provides: "--sidebar token updated to warm-dark OKLCH, --sidebar-foreground and --sidebar-border updated for contrast on dark"
      contains: "oklch(0.20 0.015 65)"
    - path: "apps/dashboard/components/layout/Sidebar.tsx"
      provides: "bg-sidebar utility on root aside; text-primary on 'crawler' brand word"
      contains: "bg-sidebar"
    - path: "apps/dashboard/components/layout/MobileNav.tsx"
      provides: "bg-sidebar on header and SheetContent; text-primary on brand"
      contains: "bg-sidebar"
    - path: "apps/dashboard/components/layout/NavLinks.tsx"
      provides: "bg-primary on active item"
      contains: "bg-primary"
    - path: "apps/dashboard/components/entries/CategoryFilterTiles.tsx"
      provides: "bg-primary border-primary on active tile state for all 5 categories"
      contains: "bg-primary border-primary text-primary-foreground"
    - path: "apps/dashboard/components/entries/HeroSection.tsx"
      provides: "text-foreground heading + text-muted-foreground subcopy + text-primary on accent word"
      contains: "text-foreground"
    - path: "apps/dashboard/components/search/HeroSearchInput.tsx"
      provides: "border-border + focus:ring-primary/40 + focus:border-primary"
      contains: "focus:ring-primary/40"
    - path: "apps/dashboard/components/layout/PageHeader.tsx"
      provides: "text-foreground title + text-muted-foreground description"
      contains: "text-foreground"
  key_links:
    - from: "apps/dashboard/components/layout/Sidebar.tsx"
      to: "apps/dashboard/app/globals.css --sidebar"
      via: "bg-sidebar Tailwind utility resolves via @theme inline --color-sidebar bridge"
      pattern: "bg-sidebar"
    - from: "apps/dashboard/components/layout/NavLinks.tsx"
      to: "apps/dashboard/app/globals.css --primary"
      via: "bg-primary Tailwind utility resolves via @theme inline --color-primary bridge"
      pattern: "bg-primary"
    - from: "apps/dashboard/components/entries/HeroSection.tsx"
      to: "apps/dashboard/app/globals.css --foreground / --muted-foreground / --primary"
      via: "text-foreground / text-muted-foreground / text-primary utility classes"
      pattern: "text-(foreground|muted-foreground|primary)"
---

<objective>
Formalize the Variant B coral + warm-dark palette into CSS variable tokens (`--sidebar`, `--sidebar-foreground`, `--sidebar-border`) and replace every hardcoded hex / zinc-* class in the layout, nav, hero, and search components with the corresponding semantic Tailwind utility (`bg-sidebar`, `bg-primary`, `text-primary`, `text-foreground`, `text-muted-foreground`, `border-border`, `ring-primary`).

Purpose: Per D-02, locks the design palette into the CSS variable system so future palette changes happen in one place (`globals.css`) and downstream components automatically inherit them. This also unblocks Plan 02 (`<mark>` uses `bg-primary/10`) and Plan 04 (Playwright baselines must reflect the formalized palette).

Output: A single `globals.css` token update + 7 component file edits that compile cleanly and render the same Variant B look as commit 944bc81, but driven by semantic tokens instead of inline hex literals.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-frontend-design-refresh/13-CONTEXT.md
@.planning/phases/13-frontend-design-refresh/13-RESEARCH.md
@.planning/phases/13-frontend-design-refresh/13-UI-SPEC.md

<interfaces>
<!-- Tailwind v4 dual-registration pattern in globals.css -->
<!-- @theme inline bridges CSS vars to Tailwind utility names -->
<!-- :root {} defines the actual values -->

Current globals.css @theme inline (lines 17-58) ALREADY bridges:
  --color-sidebar:           var(--sidebar)            (enables bg-sidebar / text-sidebar)
  --color-sidebar-foreground: var(--sidebar-foreground) (enables text-sidebar-foreground)
  --color-sidebar-border:    var(--sidebar-border)     (enables border-sidebar-border)
  --color-primary:           var(--primary)            (enables bg-primary / text-primary / ring-primary / border-primary)
  --color-primary-foreground:var(--primary-foreground) (enables text-primary-foreground)
  --color-foreground:        var(--foreground)         (enables text-foreground)
  --color-muted-foreground:  var(--muted-foreground)   (enables text-muted-foreground)
  --color-border:            var(--border)             (enables border-border)

CRITICAL: All the utility classes used in this plan (bg-sidebar, bg-primary, text-primary,
text-foreground, text-muted-foreground, border-border, ring-primary, border-primary)
are ALREADY enabled by the existing @theme inline block. No new @theme entries are needed.

Only :root {} values need to change.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update sidebar CSS variables in globals.css (D-02)</name>
  <files>apps/dashboard/app/globals.css</files>
  <read_first>
    - apps/dashboard/app/globals.css (current :root block at lines 60-93, .dark block at lines 95-127 — confirm dual definition pattern)
    - .planning/phases/13-frontend-design-refresh/13-UI-SPEC.md (Color section, lines 117-141 for the exact OKLCH target values)
  </read_first>
  <action>
    In `apps/dashboard/app/globals.css`, modify ONLY the three sidebar-related lines inside the `:root {}` block (currently lines 85-91). Replace with these EXACT values:

    Before:
    ```css
    --sidebar: oklch(0.985 0 0);
    --sidebar-foreground: oklch(0.145 0 0);
    --sidebar-primary: oklch(0.55 0.19 28);
    --sidebar-primary-foreground: oklch(0.985 0 0);
    --sidebar-accent: oklch(0.97 0 0);
    --sidebar-accent-foreground: oklch(0.205 0 0);
    --sidebar-border: oklch(0.922 0 0);
    --sidebar-ring: oklch(0.55 0.19 28);
    ```

    After:
    ```css
    --sidebar: oklch(0.20 0.015 65);              /* #252017 warm-dark (D-02) */
    --sidebar-foreground: oklch(0.85 0.005 80);   /* warm off-white text on dark sidebar */
    --sidebar-primary: oklch(0.55 0.19 28);       /* coral — unchanged */
    --sidebar-primary-foreground: oklch(0.985 0 0); /* unchanged */
    --sidebar-accent: oklch(1 0 0 / 6%);          /* white/6 hover bg for nav items on dark sidebar */
    --sidebar-accent-foreground: oklch(0.985 0 0); /* white text on hover */
    --sidebar-border: oklch(1 0 0 / 8%);          /* white/8 dividers on dark sidebar */
    --sidebar-ring: oklch(0.55 0.19 28);          /* coral focus ring — unchanged */
    ```

    Do NOT modify the `.dark` block — dark mode is out of scope per CONTEXT.md Deferred Ideas. Do NOT modify any other variables (background, foreground, primary, chart-*, etc. all stay as-is).
    Do NOT touch the `@theme inline` block — `--color-sidebar` and related bridges already exist.
  </action>
  <verify>
    <automated>cd apps/dashboard && grep -E "^\s*--sidebar:\s*oklch\(0\.20 0\.015 65\)" app/globals.css && grep -E "^\s*--sidebar-foreground:\s*oklch\(0\.85 0\.005 80\)" app/globals.css && grep -E "^\s*--sidebar-border:\s*oklch\(1 0 0 / 8%\)" app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `apps/dashboard/app/globals.css` line within :root contains `--sidebar: oklch(0.20 0.015 65)` (grep -E `^\s*--sidebar:\s*oklch\(0\.20 0\.015 65\)` returns the line)
    - `apps/dashboard/app/globals.css` contains `--sidebar-foreground: oklch(0.85 0.005 80)`
    - `apps/dashboard/app/globals.css` contains `--sidebar-border: oklch(1 0 0 / 8%)`
    - `apps/dashboard/app/globals.css` contains `--sidebar-accent: oklch(1 0 0 / 6%)`
    - `apps/dashboard/app/globals.css` `.dark` block is unchanged (still contains `--sidebar: oklch(0.205 0 0)` at line 119)
    - `apps/dashboard/app/globals.css` `@theme inline` block is unchanged (still contains `--color-sidebar: var(--sidebar);`)
    - No other `:root {}` variables modified (`--primary`, `--background`, `--chart-1` through `--chart-5`, `--border`, `--muted-foreground`, `--radius` unchanged)
  </acceptance_criteria>
  <done>globals.css :root block has the three updated sidebar lines and the dark block is untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace hardcoded hex classes with semantic tokens in layout + entries components (D-02)</name>
  <files>
    apps/dashboard/components/layout/Sidebar.tsx,
    apps/dashboard/components/layout/MobileNav.tsx,
    apps/dashboard/components/layout/NavLinks.tsx,
    apps/dashboard/components/layout/PageHeader.tsx,
    apps/dashboard/components/entries/HeroSection.tsx,
    apps/dashboard/components/entries/CategoryFilterTiles.tsx,
    apps/dashboard/components/search/HeroSearchInput.tsx
  </files>
  <read_first>
    - apps/dashboard/components/layout/Sidebar.tsx (full file — only 26 lines)
    - apps/dashboard/components/layout/MobileNav.tsx (full file — only 42 lines; has 2 occurrences of bg-[#1c1814] and 2 of text-[#d8553a])
    - apps/dashboard/components/layout/NavLinks.tsx (full file — only 43 lines; one bg-[#d8553a] on active state)
    - apps/dashboard/components/layout/PageHeader.tsx (full file — 19 lines; text-zinc-900 + text-zinc-500)
    - apps/dashboard/components/entries/HeroSection.tsx (full file — 41 lines; border-zinc-200 + bg-white + text-zinc-900 + text-zinc-500 + text-[#d8553a])
    - apps/dashboard/components/entries/CategoryFilterTiles.tsx (full file — 98 lines; 5 occurrences of `bg-[#d8553a] border-[#d8553a] text-white`)
    - apps/dashboard/components/search/HeroSearchInput.tsx (full file — 90 lines; border-zinc-200, focus:ring-[#d8553a]/40, focus:border-[#d8553a], bg-zinc-100 on kbd hint)
    - .planning/phases/13-frontend-design-refresh/13-UI-SPEC.md (Token Formalization Map, lines 528-561, has the complete file-by-file replacement list)
  </read_first>
  <action>
    Make EXACTLY the following string replacements. Each is a literal find-and-replace of one class string.

    **File: apps/dashboard/components/layout/Sidebar.tsx (line 7)**
      Replace `bg-[#1c1814]` with `bg-sidebar` (in the `<aside>` className).
      Replace `text-[#d8553a]` with `text-primary` (line 13 — inside the brand span).
      Replace `text-white` (line 12 — inside the "web" brand span) with `text-sidebar-foreground` so the brand color tracks the sidebar foreground token.
      Replace `border-white/8` (line 9 — logo container border) with `border-sidebar-border`.
      Replace `border-white/8` (line 19 — search divider) with `border-sidebar-border`.

    **File: apps/dashboard/components/layout/MobileNav.tsx**
      Line 14 `<header>` className: replace `bg-[#1c1814]` with `bg-sidebar`. Keep `border-b border-zinc-200` AS-IS for this task (the visible bottom border of the mobile header stays — Plan 03 handles management-table zinc tokens; this file is layout-only and the mobile bottom border is intentionally light).
      Line 19 `<SheetContent>` className: replace `bg-[#1c1814]` with `bg-sidebar`.
      Line 20 `<SheetHeader>` className: replace `border-white/8` with `border-sidebar-border`.
      Line 23 brand "web" span: replace `text-white` with `text-sidebar-foreground`.
      Line 24 brand "crawler" span: replace `text-[#d8553a]` with `text-primary`.
      Line 26 search divider: replace `border-white/8` with `border-sidebar-border`.
      Line 34 brand "web" span: replace `text-white` with `text-sidebar-foreground`.
      Line 35 brand "crawler" span: replace `text-[#d8553a]` with `text-primary`.

    **File: apps/dashboard/components/layout/NavLinks.tsx (line 31)**
      Replace `'bg-[#d8553a] text-white font-medium'` with `'bg-primary text-primary-foreground font-medium'`.
      Line 32 (inactive state): replace `'text-zinc-400 hover:bg-white/6 hover:text-zinc-100'` with `'text-sidebar-foreground/70 hover:bg-sidebar-accent hover:text-sidebar-foreground'`.
      Line 35 (icon inactive): replace `'text-zinc-500'` with `'text-sidebar-foreground/50'`. Keep `'text-white'` for active — change to `'text-primary-foreground'`.

    **File: apps/dashboard/components/layout/PageHeader.tsx (lines 13-14)**
      Replace `text-zinc-900` with `text-foreground` (on the h1).
      Replace `text-zinc-500` with `text-muted-foreground` (on the description p).

    **File: apps/dashboard/components/entries/HeroSection.tsx**
      Line 13: replace `border-zinc-200 bg-white` with `border-border bg-card`.
      Line 16: replace `text-zinc-900` with `text-foreground` (on the h1).
      Line 18: replace `text-[#d8553a]` with `text-primary` (on the accent word "crawled?").
      Line 20: replace `text-zinc-500` with `text-muted-foreground` (on the description p).

    **File: apps/dashboard/components/entries/CategoryFilterTiles.tsx**
      Each of the 5 CATEGORIES entries (lines 14, 21, 28, 35, 42) has `colorActive: 'bg-[#d8553a] border-[#d8553a] text-white'`.
      Replace each occurrence with `colorActive: 'bg-primary border-primary text-primary-foreground'`.
      Do NOT touch the `colorInactive` strings — those are deliberate per-category pastel palettes.

    **File: apps/dashboard/components/search/HeroSearchInput.tsx**
      Line 47 (Search icon): replace `text-zinc-400` with `text-muted-foreground`.
      Line 59 `<input>` className: replace `border-zinc-200 bg-white` with `border-input bg-card`.
      Line 60: replace `text-zinc-900 placeholder:text-zinc-400` with `text-foreground placeholder:text-muted-foreground`.
      Line 62 focus ring: replace `focus:ring-[#d8553a]/40 focus:border-[#d8553a]` with `focus:ring-primary/40 focus:border-primary`.
      Line 66 (kbd hint span): replace `text-zinc-400 font-mono bg-zinc-100` with `text-muted-foreground font-mono bg-muted` and replace `border-zinc-200` with `border-border`.
      Line 82 (Suspense fallback): replace `border-zinc-200 bg-zinc-100` with `border-border bg-muted`.

    Do NOT change any other classes, JSX structure, imports, props, or logic in any file. The visual intent must remain Variant B — only the token names change.
  </action>
  <verify>
    <automated>cd apps/dashboard && ! grep -RE "bg-\[#1c1814\]|text-\[#d8553a\]|bg-\[#d8553a\]|focus:ring-\[#d8553a\]|focus:border-\[#d8553a\]" components/layout components/entries components/search && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -RE "bg-\[#1c1814\]|text-\[#d8553a\]|bg-\[#d8553a\]" apps/dashboard/components/layout apps/dashboard/components/entries apps/dashboard/components/search` returns ZERO matches
    - `grep -E "focus:ring-\[#d8553a\]|focus:border-\[#d8553a\]" apps/dashboard/components/search/HeroSearchInput.tsx` returns ZERO matches
    - `grep -c "bg-sidebar" apps/dashboard/components/layout/Sidebar.tsx` returns 1 or more
    - `grep -c "bg-sidebar" apps/dashboard/components/layout/MobileNav.tsx` returns 2 or more (header + SheetContent)
    - `grep -c "text-primary" apps/dashboard/components/layout/MobileNav.tsx` returns 2 or more (two brand spans)
    - `grep -c "text-primary" apps/dashboard/components/layout/Sidebar.tsx` returns 1
    - `grep -c "bg-primary text-primary-foreground" apps/dashboard/components/layout/NavLinks.tsx` returns 1
    - `grep -c "bg-primary border-primary text-primary-foreground" apps/dashboard/components/entries/CategoryFilterTiles.tsx` returns 5
    - `grep -c "text-foreground" apps/dashboard/components/layout/PageHeader.tsx` returns 1
    - `grep -c "text-muted-foreground" apps/dashboard/components/layout/PageHeader.tsx` returns 1
    - `grep -c "text-foreground" apps/dashboard/components/entries/HeroSection.tsx` returns 1
    - `grep -c "text-primary" apps/dashboard/components/entries/HeroSection.tsx` returns 1
    - `grep -c "border-border bg-card" apps/dashboard/components/entries/HeroSection.tsx` returns 1
    - `grep -c "focus:ring-primary/40 focus:border-primary" apps/dashboard/components/search/HeroSearchInput.tsx` returns 1
    - `pnpm --filter @web-crawler/dashboard type-check` exits 0 (no new TS errors)
    - `pnpm --filter @web-crawler/dashboard test` exits 0 (existing 8 vitest files still pass)
  </acceptance_criteria>
  <done>All seven component files compile, all listed grep checks pass, existing vitest unit tests still pass.</done>
</task>

</tasks>

<threat_model>
N/A — purely visual/CSS/TSX changes, no new data flows, auth, or external integrations.
</threat_model>

<verification>
1. `pnpm --filter @web-crawler/dashboard type-check` — passes with zero TS errors.
2. `pnpm --filter @web-crawler/dashboard test` — all existing vitest files pass.
3. `pnpm --filter @web-crawler/dashboard build` — Next.js build succeeds.
4. `pnpm --filter @web-crawler/dashboard dev` then visit http://localhost:3000/entries — sidebar renders warm-dark `#252017` (visibly lighter than the prior `#1c1814` but still warm-brown), active nav item is coral, brand "crawler" text is coral, hero "crawled?" word is coral, search input focus shows coral ring, category tile active state is coral. Mobile width (≤768px) — header bar is also warm-dark, drawer is warm-dark, both brands show coral on "crawler".
5. `grep -RE "bg-\[#1c1814\]|text-\[#d8553a\]|bg-\[#d8553a\]|focus:ring-\[#d8553a\]|focus:border-\[#d8553a\]" apps/dashboard/components/layout apps/dashboard/components/entries apps/dashboard/components/search` — returns nothing.
</verification>

<success_criteria>
- globals.css :root has the three new sidebar token values (sidebar / sidebar-foreground / sidebar-border) plus updated sidebar-accent.
- Sidebar.tsx, MobileNav.tsx (×2 instances), NavLinks.tsx, PageHeader.tsx, HeroSection.tsx, CategoryFilterTiles.tsx (×5 active states), HeroSearchInput.tsx all use semantic Tailwind tokens — zero `bg-[#1c1814]` / `text-[#d8553a]` / `bg-[#d8553a]` / `focus:ring-[#d8553a]` / `focus:border-[#d8553a]` matches remain in these directories.
- `pnpm type-check`, `pnpm test`, and `pnpm build` all succeed.
- Variant B look is visually identical (modulo intentional warm-dark nudge from `#1c1814` to `#252017`) — coral active nav, coral category tile active state, coral brand "crawler", coral hero accent word, coral focus ring on hero search.
</success_criteria>

<output>
After completion, create `.planning/phases/13-frontend-design-refresh/13-01-SUMMARY.md` documenting:
- Files changed and which hardcoded values were swapped for which tokens
- The new globals.css :root sidebar values
- Visual diff notes (sidebar lighter? still warm-brown?)
- Any deviations from the UI-SPEC token map (should be none)
- Type-check / test / build status
</output>
