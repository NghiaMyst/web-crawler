---
phase: 13
plan: 04
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
  - 13-03
files_modified:
  - apps/dashboard/package.json
  - apps/dashboard/playwright.config.ts
  - apps/dashboard/e2e/visual.spec.ts
  - apps/dashboard/e2e/__snapshots__/
  - apps/dashboard/.gitignore
autonomous: false
requirements:
  - DESIGN-01
  - DESIGN-04
user_setup: []
must_haves:
  truths:
    - "@playwright/test is installed as a dev dependency in apps/dashboard/package.json at version ^1.48"
    - "Chromium browser is installed locally via `npx playwright install chromium`"
    - "apps/dashboard/playwright.config.ts exists with testDir './e2e', baseURL http://localhost:3000, Desktop Chrome at 1280x800, webServer command 'pnpm dev', and expect.toHaveScreenshot.maxDiffPixelRatio: 0.02"
    - "apps/dashboard/e2e/visual.spec.ts exists with four screenshot tests covering /entries, /charts, /sources, /jobs"
    - "Running `npx playwright test --update-snapshots` from apps/dashboard/ generates baseline PNGs under e2e/__snapshots__/ which are committed to the repo"
    - "Re-running `npx playwright test` (without --update-snapshots) on the same code passes — proving the baseline is stable"
    - "User has visually approved the captured baselines (this plan is non-autonomous because the human must confirm the Variant B look is correct before the baselines lock in)"
  artifacts:
    - path: "apps/dashboard/playwright.config.ts"
      provides: "Playwright config — testDir, Chromium device, webServer, screenshot tolerance"
      contains: "maxDiffPixelRatio: 0.02"
    - path: "apps/dashboard/e2e/visual.spec.ts"
      provides: "Four screenshot tests (entries, charts, sources, jobs)"
      contains: "toHaveScreenshot"
    - path: "apps/dashboard/e2e/__snapshots__/"
      provides: "Baseline PNGs (one per page-spec combination)"
      contains_files: "visual.spec.ts-snapshots/"
    - path: "apps/dashboard/package.json"
      provides: "@playwright/test ^1.48 devDependency + 'test:e2e' script"
      contains: '"@playwright/test"'
  key_links:
    - from: "apps/dashboard/playwright.config.ts"
      to: "Next.js dev server"
      via: "webServer.command 'pnpm dev', url 'http://localhost:3000'"
      pattern: "webServer:"
    - from: "apps/dashboard/e2e/visual.spec.ts"
      to: "apps/dashboard/e2e/__snapshots__/"
      via: "expect(page).toHaveScreenshot('{name}.png', { fullPage: true })"
      pattern: "toHaveScreenshot"
---

<objective>
Install Playwright into the dashboard app, write a `playwright.config.ts` plus four screenshot tests, and generate committed baseline snapshots for the Variant B redesign — gated by a human checkpoint so the user can visually verify the four captured pages before the baselines lock in (D-10).

Purpose: Closes D-10 (Playwright visual QA) and provides regression detection for all future dashboard work. Must run AFTER Plans 01-03 so the snapshots capture the final Variant B palette — capturing them earlier would lock in obsolete colors and force a re-baseline. The human checkpoint exists because screenshot tests fail loudly on unexpected diff; the user must approve "yes this is the look we want" before committing PNGs.

Output: An installed `@playwright/test` dev dependency, a `playwright.config.ts`, four screenshot tests, committed baseline PNGs under `e2e/__snapshots__/`, and an updated `.gitignore` that keeps `e2e/__snapshots__/` tracked while ignoring Playwright runtime artifacts (`test-results/`, `playwright-report/`).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-frontend-design-refresh/13-CONTEXT.md
@.planning/phases/13-frontend-design-refresh/13-RESEARCH.md
@.planning/phases/13-frontend-design-refresh/13-UI-SPEC.md
@.planning/phases/13-frontend-design-refresh/13-VALIDATION.md
@.planning/phases/13-frontend-design-refresh/13-01-SUMMARY.md
@.planning/phases/13-frontend-design-refresh/13-02-SUMMARY.md
@.planning/phases/13-frontend-design-refresh/13-03-SUMMARY.md

<interfaces>
<!-- Playwright config pattern (UI-SPEC Playwright Snapshot Strategy lines 470-522): -->

// apps/dashboard/playwright.config.ts
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  snapshotDir: './e2e/__snapshots__',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  reporter: process.env.CI ? 'github' : 'list',
  use: {
    baseURL: 'http://localhost:3000',
    ...devices['Desktop Chrome'],
    viewport: { width: 1280, height: 800 },
    trace: 'retain-on-failure',
  },
  webServer: {
    command: 'pnpm dev',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
    timeout: 60_000,
  },
  expect: {
    toHaveScreenshot: { maxDiffPixelRatio: 0.02 },
  },
});

<!-- Test pattern (UI-SPEC Playwright Snapshot Strategy "Test scope" lines 487-494): -->

// apps/dashboard/e2e/visual.spec.ts
import { test, expect } from '@playwright/test';

const PAGES = [
  { path: '/entries', name: 'entries' },
  { path: '/charts',  name: 'charts'  },
  { path: '/sources', name: 'sources' },
  { path: '/jobs',    name: 'jobs'    },
];

for (const { path, name } of PAGES) {
  test(`${name} page renders Variant B layout`, async ({ page }) => {
    await page.goto(path);
    await page.waitForLoadState('networkidle');
    await expect(page).toHaveScreenshot(`${name}.png`, { fullPage: true });
  });
}

<!-- Empty-state behavior: dev server runs WITHOUT the .NET API by default. -->
<!-- This means: -->
<!-- - /entries shows the EntriesTable empty state (Inbox icon — Plan 02) -->
<!-- - /charts shows the VolumeChart empty state (BarChart3 icon — Plan 03) -->
<!-- - /sources shows SourcesEmptyState component -->
<!-- - /jobs shows JobsEmptyState component -->
<!-- These are the deliberate baseline images — they prove the layout/palette renders correctly even without data. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install Playwright + Chromium and create playwright.config.ts + e2e/visual.spec.ts (D-10 Wave 0)</name>
  <files>
    apps/dashboard/package.json,
    apps/dashboard/playwright.config.ts,
    apps/dashboard/e2e/visual.spec.ts,
    apps/dashboard/.gitignore
  </files>
  <read_first>
    - apps/dashboard/package.json (current dependencies + scripts; verify there is no existing @playwright/test entry)
    - apps/dashboard/.gitignore (current ignore rules; confirm whether `test-results/` / `playwright-report/` are already listed — if absent, add them)
    - .planning/phases/13-frontend-design-refresh/13-UI-SPEC.md (Playwright Snapshot Strategy section lines 470-522)
    - .planning/phases/13-frontend-design-refresh/13-VALIDATION.md (Wave 0 Requirements lines 58-65)
  </read_first>
  <action>
    **Step A — Install dependency.** From the repo root, run:
    ```bash
    pnpm add -D @playwright/test --filter @web-crawler/dashboard
    ```
    This adds `@playwright/test` (resolves to a recent 1.4x version) to `apps/dashboard/package.json` `devDependencies` and updates `pnpm-lock.yaml`. Verify with `cat apps/dashboard/package.json | grep playwright` — must show the new entry.

    **Step B — Install Chromium binary locally.** Run:
    ```bash
    npx playwright install chromium
    ```
    This downloads the Chromium binary into the OS-local Playwright cache. Do NOT install other browsers (Firefox/WebKit) — D-10 scope is Chrome headless only.

    **Step C — Add an e2e script to `apps/dashboard/package.json`.** Insert two new scripts inside the existing `"scripts"` block:
    ```json
    "test:e2e": "playwright test",
    "test:e2e:update": "playwright test --update-snapshots"
    ```
    Keep all existing scripts (`build`, `dev`, `lint`, `type-check`, `test`) untouched.

    **Step D — Create `apps/dashboard/playwright.config.ts`.** Write EXACTLY this content:
    ```typescript
    import { defineConfig, devices } from '@playwright/test';

    /**
     * Playwright configuration for visual regression snapshots (Phase 13 D-10).
     * Single project: Chromium headless at 1280x800 (Desktop Chrome).
     * The dev server is started automatically via the webServer block.
     */
    export default defineConfig({
      testDir: './e2e',
      snapshotDir: './e2e/__snapshots__',
      fullyParallel: true,
      forbidOnly: !!process.env.CI,
      retries: process.env.CI ? 1 : 0,
      reporter: process.env.CI ? 'github' : 'list',
      use: {
        baseURL: 'http://localhost:3000',
        ...devices['Desktop Chrome'],
        viewport: { width: 1280, height: 800 },
        trace: 'retain-on-failure',
      },
      webServer: {
        command: 'pnpm dev',
        url: 'http://localhost:3000',
        reuseExistingServer: !process.env.CI,
        timeout: 60_000,
      },
      expect: {
        toHaveScreenshot: { maxDiffPixelRatio: 0.02 },
      },
    });
    ```

    **Step E — Create `apps/dashboard/e2e/visual.spec.ts`.** First `mkdir -p apps/dashboard/e2e`. Then write EXACTLY this content:
    ```typescript
    import { test, expect } from '@playwright/test';

    /**
     * Visual regression snapshots — Phase 13 D-10.
     *
     * Captures each of the four key pages with the dashboard's empty state
     * (no API required). This proves the Variant B layout + palette renders
     * correctly even before crawl data is present.
     *
     * Baselines live in e2e/__snapshots__/ and are committed to the repo.
     * Regenerate with: pnpm test:e2e:update
     */

    const PAGES = [
      { path: '/entries', name: 'entries' },
      { path: '/charts',  name: 'charts'  },
      { path: '/sources', name: 'sources' },
      { path: '/jobs',    name: 'jobs'    },
    ] as const;

    for (const { path, name } of PAGES) {
      test(`${name} page matches Variant B baseline`, async ({ page }) => {
        await page.goto(path);
        await page.waitForLoadState('networkidle');
        // Allow font loading + suspense fallback resolution.
        await page.waitForTimeout(500);
        await expect(page).toHaveScreenshot(`${name}.png`, {
          fullPage: true,
        });
      });
    }
    ```

    **Step F — Update `apps/dashboard/.gitignore`.** Append (do not remove existing rules) the following lines if they are not already present:
    ```
    # Playwright
    /test-results/
    /playwright-report/
    /blob-report/
    /playwright/.cache/
    ```
    Do NOT add `e2e/__snapshots__/` — baselines MUST be committed. Do NOT add `e2e/` itself — the spec file is committed.
  </action>
  <verify>
    <automated>cd apps/dashboard && grep -q '"@playwright/test"' package.json && grep -q '"test:e2e"' package.json && [ -f playwright.config.ts ] && [ -f e2e/visual.spec.ts ] && grep -q "maxDiffPixelRatio: 0.02" playwright.config.ts && grep -q "viewport: { width: 1280, height: 800 }" playwright.config.ts && grep -q "toHaveScreenshot" e2e/visual.spec.ts && grep -q "/entries" e2e/visual.spec.ts && grep -q "/charts" e2e/visual.spec.ts && grep -q "/sources" e2e/visual.spec.ts && grep -q "/jobs" e2e/visual.spec.ts && grep -q "/test-results/" .gitignore && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - `apps/dashboard/package.json` contains `"@playwright/test"` in `devDependencies`
    - `apps/dashboard/package.json` `scripts` block contains `"test:e2e": "playwright test"` and `"test:e2e:update": "playwright test --update-snapshots"`
    - `apps/dashboard/playwright.config.ts` exists and contains `testDir: './e2e'`, `snapshotDir: './e2e/__snapshots__'`, `baseURL: 'http://localhost:3000'`, `viewport: { width: 1280, height: 800 }`, `webServer: { command: 'pnpm dev' …`, `maxDiffPixelRatio: 0.02`
    - `apps/dashboard/e2e/visual.spec.ts` exists and contains `toHaveScreenshot` plus exactly the four routes `/entries`, `/charts`, `/sources`, `/jobs`
    - `apps/dashboard/.gitignore` contains `/test-results/` and `/playwright-report/`
    - `apps/dashboard/.gitignore` does NOT contain `e2e/__snapshots__/` or any glob that would ignore PNGs under `e2e/`
    - `npx playwright --version` reports a version ≥ 1.48 (Chromium binary installed)
    - `pnpm --filter @web-crawler/dashboard type-check` exits 0 (the new TS files type-check cleanly)
    - The Vitest test suite (`pnpm test`) is unaffected and still passes (the `e2e/` directory is outside vitest's default include — verify by re-running `pnpm test`)
  </acceptance_criteria>
  <done>Playwright installed, config + spec file in place, .gitignore updated, type-check passes, existing vitest still passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Generate baseline PNGs via update-snapshots, then re-run to verify stability</name>
  <files>apps/dashboard/e2e/__snapshots__/</files>
  <read_first>
    - apps/dashboard/playwright.config.ts (confirm webServer + snapshotDir from Task 1)
    - apps/dashboard/e2e/visual.spec.ts (confirm the four routes from Task 1)
    - .planning/phases/13-frontend-design-refresh/13-RESEARCH.md (Pitfall 3 — first run with --update-snapshots is required to generate baselines)
  </read_first>
  <action>
    **Step A — Generate the baseline images.** From `apps/dashboard/`, run:
    ```bash
    pnpm test:e2e:update
    ```
    Playwright will:
    1. Start the Next.js dev server (`pnpm dev`) and wait for `http://localhost:3000` to respond.
    2. Run each of the four tests in `visual.spec.ts`.
    3. Generate PNGs under `apps/dashboard/e2e/__snapshots__/visual.spec.ts-snapshots/` (Playwright's default per-spec subdirectory).
    4. Shut down the dev server.

    Expected result: all four tests pass on the FIRST run because `--update-snapshots` writes baselines instead of comparing. Four PNG files exist under `e2e/__snapshots__/visual.spec.ts-snapshots/`:
    - `entries-{platform}-chromium.png`
    - `charts-{platform}-chromium.png`
    - `sources-{platform}-chromium.png`
    - `jobs-{platform}-chromium.png`
    (Playwright postfixes with the OS — e.g. `entries-linux.png` on linux/amd64. The naming is automatic; do not rename.)

    **Step B — Re-run WITHOUT --update-snapshots to prove stability.** From `apps/dashboard/`, run:
    ```bash
    pnpm test:e2e
    ```
    Expected result: all four tests PASS (zero diff against the just-generated baselines). If any test fails on this second run with a non-trivial diff, investigate:
    - Are fonts still loading? (consider raising `waitForTimeout` in the spec)
    - Is there an animation in the page that isn't deterministic? (if so, add `page.addStyleTag({ content: '*, *::before, *::after { transition: none !important; animation: none !important; }' })` BEFORE the screenshot call)
    - Is the connection-dot blinking? (consider mocking SignalR state)

    If the failure points to a real non-determinism source, add a minimal `beforeEach` hook to disable animations and re-run BOTH Step A and Step B. Do NOT raise `maxDiffPixelRatio` above 0.02 to paper over real issues — solve the source.

    **Step C — Verify the baselines exist and are not empty.** Run:
    ```bash
    ls -la apps/dashboard/e2e/__snapshots__/visual.spec.ts-snapshots/
    ```
    Confirm four `.png` files each ≥ 5 KB (a near-empty PNG would be ~1 KB — that signals a render failure).
  </action>
  <verify>
    <automated>cd apps/dashboard && [ "$(find e2e/__snapshots__ -name '*.png' -size +4k 2>/dev/null | wc -l)" -ge 4 ] && pnpm test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `apps/dashboard/e2e/__snapshots__/visual.spec.ts-snapshots/` directory exists
    - At least 4 `.png` files under that directory, each ≥ 5 KB (`find apps/dashboard/e2e/__snapshots__ -name '*.png' -size +4k | wc -l` ≥ 4)
    - The four file names contain `entries`, `charts`, `sources`, `jobs` (one each — `ls apps/dashboard/e2e/__snapshots__/visual.spec.ts-snapshots/ | grep -E '^(entries|charts|sources|jobs)' | wc -l` ≥ 4)
    - `pnpm test:e2e` (Step B re-run without --update-snapshots) exits 0 — all four tests pass against the freshly generated baselines
    - The four baseline PNGs are NOT git-ignored (`git check-ignore apps/dashboard/e2e/__snapshots__/visual.spec.ts-snapshots/entries-linux.png` returns non-zero exit and empty stdout, meaning "not ignored")
  </acceptance_criteria>
  <done>Four stable baseline PNGs generated and committed-eligible; second e2e run is green; no rebaseline needed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual approval of captured baselines (DESIGN-01)</name>
  <files>apps/dashboard/e2e/__snapshots__/visual.spec.ts-snapshots/</files>
  <read_first>
    - apps/dashboard/e2e/__snapshots__/visual.spec.ts-snapshots/ (open each .png in an image viewer)
    - design/design-v1/wireframes.jsx (Variant B reference, lines 151-250)
    - .planning/phases/13-frontend-design-refresh/13-CONTEXT.md (decisions D-01 through D-10)
  </read_first>
  <action>
    **This is a human checkpoint. Claude must STOP and present the four PNG baselines to the user for visual approval.**

    Playwright is now installed, configured for Chrome headless at 1280x800, and has captured four baseline PNGs:
    1. `entries-linux.png` — `/entries` page (Inbox empty state with hero + category tiles)
    2. `charts-linux.png` — `/charts` page (BarChart3 empty state inside chart card)
    3. `sources-linux.png` — `/sources` page (SourcesEmptyState component)
    4. `jobs-linux.png` — `/jobs` page (JobsEmptyState component)

    Combined with Plans 01-03, this is the locked-in look of Variant B for the dashboard. Once approved, these PNGs are committed as the regression baseline; any future visual change will require either explicit `pnpm test:e2e:update` or a code fix.

    **Verification steps for the user:**

    1. Open each baseline PNG locally — use the filesystem path under `apps/dashboard/e2e/__snapshots__/visual.spec.ts-snapshots/` or run `pnpm test:e2e --reporter=html` and inspect the HTML report at `playwright-report/index.html`.
    2. For each PNG, confirm the following Variant B traits are present:
       - **Sidebar (all four pages):** Warm-dark background (the new `#252017`, NOT the previous `#1c1814` which was heavier). Visibly warm-brown, not grey or zinc. "web**crawler**" logo where "crawler" is coral. The currently active nav item is coral filled.
       - **/entries:** Hero card (rounded-2xl, border, white-on-cream) with bold "What's been **crawled?**" headline (coral on "crawled?"), big search input, five category tile pills underneath. Below the hero, the entries table empty state shows the Inbox icon and "No entries found" message.
       - **/charts:** A date-range Select on the top right; below it, "No data to display" with BarChart3 icon centered. (Or if data is present locally: two chart cards with coral primary series.)
       - **/sources:** Empty state "No sources configured" with an "Add Source" coral button.
       - **/jobs:** Empty state "No jobs found".
       - **No yellow highlights anywhere.** No `bg-zinc-100` hardcoded headers visible (any table headers should read as muted-warm, not grey).
    3. If anything looks wrong (e.g. sidebar is still the old `#1c1814`, an active button is grey instead of coral, a chart card is missing its shadow, the empty state shows old "Adjust filters or wait..." text in plain `<div>` instead of with an icon), say so — the planner/executors will revise Plans 01-03 and re-run `pnpm test:e2e:update` before re-checking.
    4. If everything matches the Variant B intent, approve.

    Reference comparison: open `design/design-v1/wireframes.jsx` Variant B (lines 151-250) for the intended look.

    **Resume signal:** Type "approved" to confirm the baselines match Variant B intent. Otherwise describe each issue (page + element + what's wrong) and the planner will create a gap-closure plan to fix the underlying code and re-baseline.
  </action>
  <verify>
    <automated>echo "MANUAL — awaits user signal 'approved' or revision feedback. No automated verification possible for subjective visual judgment."</automated>
  </verify>
  <acceptance_criteria>
    - User has typed `approved` (or equivalent affirmative signal) confirming the four baselines match the Variant B visual intent
    - If user reports any deviation, the planner creates a gap-closure plan via `/gsd-plan-phase 13 --gaps`; the human approval is NOT considered given until a clean run passes after fixes
    - This checkpoint MUST block before downstream commits — the four baseline PNGs are only committed to git after explicit human approval
  </acceptance_criteria>
  <done>User has approved the baselines as matching the Variant B intent and the four baseline PNGs are eligible for git commit.</done>
</task>

</tasks>

<threat_model>
N/A — purely visual/CSS/TSX changes, no new data flows, auth, or external integrations.
</threat_model>

<verification>
1. `cd apps/dashboard && npx playwright --version` reports ≥ 1.48.
2. `cd apps/dashboard && pnpm test:e2e` exits 0 (all four tests pass against the committed baseline).
3. `find apps/dashboard/e2e/__snapshots__ -name '*.png' -size +4k | wc -l` returns 4.
4. `git status` shows the four baseline PNGs as new files (untracked or staged), confirming `.gitignore` does not exclude them.
5. `cd apps/dashboard && pnpm type-check` and `pnpm test` (existing vitest suite) still exit 0.
6. Human checkpoint approved.
</verification>

<success_criteria>
- `@playwright/test` is a `devDependencies` entry in `apps/dashboard/package.json`.
- `playwright.config.ts` exists with `testDir: './e2e'`, `baseURL: 'http://localhost:3000'`, `viewport: 1280x800`, `webServer.command: 'pnpm dev'`, `maxDiffPixelRatio: 0.02`.
- `e2e/visual.spec.ts` runs four screenshot tests against `/entries`, `/charts`, `/sources`, `/jobs`.
- `e2e/__snapshots__/visual.spec.ts-snapshots/` contains four committed baseline PNGs each ≥ 5 KB.
- `.gitignore` ignores `test-results/`, `playwright-report/`, `blob-report/`, `playwright/.cache/` — but NOT `e2e/__snapshots__/`.
- Re-running `pnpm test:e2e` after generation passes cleanly (proving stability).
- Human has approved the baselines as matching the Variant B intent.
</success_criteria>

<output>
After completion, create `.planning/phases/13-frontend-design-refresh/13-04-SUMMARY.md` documenting:
- Playwright version installed
- Browser binary install command output
- Path + size of each generated baseline PNG
- Output of the second `pnpm test:e2e` run (proving stability)
- Confirmation that .gitignore does not exclude the snapshot directory
- Human approval signal and any noted deviations from Variant B intent
- Commands the user can run later to regenerate baselines (`pnpm test:e2e:update`) or check for regressions (`pnpm test:e2e`)
</output>
