---
phase: 13
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - apps/dashboard/components/entries/entries-table.tsx
autonomous: true
requirements:
  - DESIGN-02
  - DESIGN-04
user_setup: []
must_haves:
  truths:
    - "Entries table rows have comfortable density — each TableCell uses py-3 (12px vertical padding), producing effective ~44px row height"
    - "When ?q= is active and at least one entry matches, matched substrings render inside a <mark> styled with coral palette — bg-primary/10 + underline decoration-primary decoration-2"
    - "When entries.length === 0 and no q, empty state renders a centered Inbox icon (lucide-react, size 36, opacity-40) above 'No entries found' heading and 'Adjust filters or wait for new crawl data.' subcopy"
    - "When entries.length === 0 and q is set, empty state renders a centered SearchX icon above 'No results for \"{q}\"' heading and 'Try a different search term or clear filters.' subcopy"
    - "The empty state container uses semantic CSS-variable classes (border-border, text-muted-foreground, text-foreground) — no zinc-* hardcodes"
    - "Dashboard still builds with zero TypeScript errors"
  artifacts:
    - path: "apps/dashboard/components/entries/entries-table.tsx"
      provides: "Comfortable row density, coral mark highlight, icon-driven empty state"
      contains: "py-3"
      contains_also: "bg-primary/10"
      contains_more: "Inbox"
  key_links:
    - from: "apps/dashboard/components/entries/entries-table.tsx"
      to: "lucide-react"
      via: "named import { Inbox, SearchX }"
      pattern: "import .* (Inbox|SearchX) .* from 'lucide-react'"
    - from: "apps/dashboard/components/entries/entries-table.tsx <mark>"
      to: "apps/dashboard/app/globals.css --primary"
      via: "bg-primary/10 + decoration-primary classes via @theme inline --color-primary bridge"
      pattern: "bg-primary/10.*decoration-primary"
---

<objective>
Polish the entries data table with three coordinated changes from CONTEXT.md:
- D-04: comfortable row density via `py-3` on every `TableCell`.
- D-05: restyle the `<mark>` search-highlight from `bg-yellow-200 dark:bg-yellow-800/50` to a coral palette (`bg-primary/10` background + `decoration-primary decoration-2 underline`).
- D-06: replace the plain-text empty state with a centered icon (`Inbox` when no data, `SearchX` when search active) + heading + sub-copy pattern from the UI-SPEC Copywriting Contract.

Purpose: Closes the three D-04/D-05/D-06 gaps identified in RESEARCH.md "What STILL NEEDS WORK". This is a single-file edit and depends on Plan 01 because the `<mark>` now resolves coral via the `--primary` CSS variable.

Output: A single revised `entries-table.tsx` that compiles cleanly and renders the new density, the coral mark, and the icon-driven empty state.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-frontend-design-refresh/13-CONTEXT.md
@.planning/phases/13-frontend-design-refresh/13-UI-SPEC.md
@.planning/phases/13-frontend-design-refresh/13-RESEARCH.md
@.planning/phases/13-frontend-design-refresh/13-01-SUMMARY.md

<interfaces>
<!-- Current entries-table.tsx structure (verified at planning time): -->
<!-- - Props: { entries: DataEntry[], q?: string } -->
<!-- - highlightMatches(text, q) returns React.ReactNode; wraps matches in <mark> -->
<!-- - formatPayloadPreview(payload, q) calls highlightMatches -->
<!-- - When entries.length === 0, returns a single div, not a Table -->
<!-- - Otherwise returns <Table> with TableHeader + TableBody mapping over entries -->

<!-- DataEntry type (apps/dashboard/types/api.ts, used here): -->
interface DataEntry {
  id: string;
  category: string;
  entryKey: string | null;
  payload: Record<string, unknown>;
  crawledAt: string; // ISO
}

<!-- lucide-react icons used in this plan: Inbox, SearchX -->
<!-- Both exist in lucide-react ^1.14.0 (already installed, verified package.json) -->

<!-- Empty-state pattern (from UI-SPEC Copywriting Contract): -->
<!--
  <div className="flex flex-col items-center justify-center rounded-lg border border-border p-12 gap-3 text-muted-foreground">
    <Icon size={36} className="opacity-40" aria-hidden="true" />
    <p className="text-sm font-medium text-foreground">{heading}</p>
    <p className="text-xs">{sub-copy}</p>
  </div>
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restyle mark and rewrite empty state in entries-table.tsx (D-05, D-06)</name>
  <files>apps/dashboard/components/entries/entries-table.tsx</files>
  <read_first>
    - apps/dashboard/components/entries/entries-table.tsx (full file — 107 lines; the current `<mark>` is on line 33-37 and the empty state is on lines 66-73)
    - apps/dashboard/types/api.ts (confirm DataEntry shape — used in props)
    - .planning/phases/13-frontend-design-refresh/13-UI-SPEC.md (Data Table section lines 313-333; Empty State Copy lines 239-256)
  </read_first>
  <action>
    Modify `apps/dashboard/components/entries/entries-table.tsx` with EXACTLY these three changes:

    **Change A — Add lucide imports (top of file, after the existing imports on lines 1-10):**
    Add a new import line: `import { Inbox, SearchX } from 'lucide-react';`
    Keep all existing imports (DataEntry type, Table/* primitives, CategoryBadge) unchanged.

    **Change B — Restyle the `<mark>` element (in `highlightMatches`):**
    Replace the JSX block on lines 33-37 (`<mark key={i} className="bg-yellow-200 dark:bg-yellow-800/50 rounded-sm px-0.5">{part}</mark>`) with:
    ```tsx
    <mark
      key={i}
      className="bg-primary/10 rounded-sm px-0.5 underline decoration-primary decoration-2 underline-offset-2 text-inherit"
    >
      {part}
    </mark>
    ```
    Keep the surrounding `parts.map((part, i) => ...)` and the `else` branch (returning the unwrapped `part`) intact.

    **Change C — Rewrite the empty-state branch (replace lines 66-74):**
    Replace this block:
    ```tsx
    if (entries.length === 0) {
      return (
        <div className="rounded-lg border border-border p-8 text-center text-muted-foreground text-sm">
          {q && q.trim() !== ''
            ? `No results for "${q.trim()}". Try a different search term or clear filters.`
            : 'No entries found. Adjust filters or wait for new crawl data.'}
        </div>
      );
    }
    ```
    With:
    ```tsx
    if (entries.length === 0) {
      const hasQuery = !!q && q.trim() !== '';
      const Icon = hasQuery ? SearchX : Inbox;
      const heading = hasQuery
        ? `No results for "${q!.trim()}"`
        : 'No entries found';
      const subcopy = hasQuery
        ? 'Try a different search term or clear filters.'
        : 'Adjust filters or wait for new crawl data.';
      return (
        <div className="flex flex-col items-center justify-center rounded-lg border border-border p-12 gap-3 text-muted-foreground">
          <Icon size={36} className="opacity-40" aria-hidden="true" />
          <p className="text-sm font-medium text-foreground">{heading}</p>
          <p className="text-xs">{subcopy}</p>
        </div>
      );
    }
    ```

    Do not modify the `Table` / `TableHeader` / `TableBody` block (lines 76-106 currently) in this task — Task 2 handles row density. Do not change the `escapeRegExp`, `highlightMatches` (other than the `<mark>` swap), `formatPayloadPreview`, or `formatDate` helpers.

    Per D-06 the icon must use `aria-hidden="true"` so screen readers read only the heading + subcopy. The heading uses `text-foreground` (not muted) for emphasis; the icon and subcopy use the inherited muted-foreground from the wrapper.
  </action>
  <verify>
    <automated>cd apps/dashboard && grep -q "import { Inbox, SearchX } from 'lucide-react'" components/entries/entries-table.tsx && grep -q "bg-primary/10 rounded-sm px-0.5 underline decoration-primary decoration-2" components/entries/entries-table.tsx && grep -q "const Icon = hasQuery ? SearchX : Inbox" components/entries/entries-table.tsx && ! grep -q "bg-yellow-200" components/entries/entries-table.tsx && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from 'lucide-react'" apps/dashboard/components/entries/entries-table.tsx` returns 1 (single new import line)
    - `grep -c "Inbox" apps/dashboard/components/entries/entries-table.tsx` returns at least 2 (import + usage)
    - `grep -c "SearchX" apps/dashboard/components/entries/entries-table.tsx` returns at least 2
    - `grep -q "bg-primary/10 rounded-sm px-0.5 underline decoration-primary decoration-2"` succeeds
    - `grep -q "bg-yellow-200"` against entries-table.tsx returns FALSE (the old yellow mark is gone)
    - `grep -q "dark:bg-yellow-800"` against entries-table.tsx returns FALSE
    - `grep -q "flex flex-col items-center justify-center rounded-lg border border-border p-12 gap-3 text-muted-foreground"` succeeds (new empty-state container)
    - `grep -q "text-sm font-medium text-foreground"` succeeds (heading class)
    - `grep -q 'aria-hidden="true"'` succeeds (icon aria attribute)
    - `pnpm --filter @web-crawler/dashboard type-check` exits 0
    - `pnpm --filter @web-crawler/dashboard test` exits 0
  </acceptance_criteria>
  <done>entries-table.tsx imports Inbox + SearchX, the `<mark>` uses coral classes, and the empty state renders icon + heading + subcopy with the exact patterns above.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply comfortable row density to entries TableCells (D-04)</name>
  <files>apps/dashboard/components/entries/entries-table.tsx</files>
  <read_first>
    - apps/dashboard/components/entries/entries-table.tsx (the rendered table block — `<TableBody>` mapping starting at line 86 currently, four TableCells per row)
    - .planning/phases/13-frontend-design-refresh/13-UI-SPEC.md (Spacing Scale section "Note on table row padding" at lines 76-78 — `py-3` not `py-3.5`)
  </read_first>
  <action>
    In `apps/dashboard/components/entries/entries-table.tsx`, add `py-3` to EACH of the four `<TableCell>` elements inside the row map. The current cells (verified at planning):

    1. `<TableCell>` (category column — currently no className): change opening tag to `<TableCell className="py-3">`.
    2. `<TableCell className="font-mono text-xs text-muted-foreground max-w-[160px] truncate">` (entry key): prepend `py-3 ` so the className becomes `"py-3 font-mono text-xs text-muted-foreground max-w-[160px] truncate"`.
    3. `<TableCell className="max-w-[400px] truncate text-xs text-muted-foreground">` (payload preview): prepend `py-3 ` → `"py-3 max-w-[400px] truncate text-xs text-muted-foreground"`.
    4. `<TableCell className="text-xs text-muted-foreground">` (crawled at): prepend `py-3 ` → `"py-3 text-xs text-muted-foreground"`.

    Do NOT change the TableHead cells in the header — D-04 applies to body row density only (the header retains its default Shadcn padding).

    Do NOT use `py-3.5` — UI-SPEC anti-pattern #8 forbids 14px (non-multiple-of-4). Do NOT use a global wrapper trick like `[&_td]:py-3` — apply the class explicitly to each cell so the change is auditable per cell.

    Per D-04 the goal is ~48px row height for a comfortable feel; `py-3` (12px top + 12px bottom = 24px cell padding) combined with the inherent line-height (~20px for text-sm and text-xs) yields ~44-48px effective row height. This matches the UI-SPEC spacing scale note.
  </action>
  <verify>
    <automated>cd apps/dashboard && [ "$(grep -c 'py-3' components/entries/entries-table.tsx)" -ge 4 ] && ! grep -q 'py-3\.5' components/entries/entries-table.tsx && pnpm type-check && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "py-3"` against entries-table.tsx returns 4 or more (one per data cell)
    - `grep -c "py-3\."` against entries-table.tsx returns 0 (no py-3.5)
    - `grep -q '<TableCell className="py-3">'` succeeds (category cell)
    - `grep -q 'className="py-3 font-mono text-xs text-muted-foreground max-w-\[160px\] truncate"'` succeeds (entry key cell)
    - `grep -q 'className="py-3 max-w-\[400px\] truncate text-xs text-muted-foreground"'` succeeds (payload cell)
    - `grep -q 'className="py-3 text-xs text-muted-foreground"'` succeeds (crawled-at cell)
    - `<TableHead>` lines (4 of them) are unchanged — `grep -c "py-3" components/entries/entries-table.tsx` excludes the head lines, i.e. `grep "TableHead.*py-3"` returns nothing
    - `pnpm --filter @web-crawler/dashboard type-check` exits 0
    - `pnpm --filter @web-crawler/dashboard test` exits 0
  </acceptance_criteria>
  <done>Each of the four body TableCells has the `py-3` prefix; no TableHead lines were touched; type-check and tests pass.</done>
</task>

</tasks>

<threat_model>
N/A — purely visual/CSS/TSX changes, no new data flows, auth, or external integrations.
</threat_model>

<verification>
1. `pnpm --filter @web-crawler/dashboard type-check` — passes.
2. `pnpm --filter @web-crawler/dashboard test` — passes (existing 8 vitest specs unaffected).
3. `pnpm --filter @web-crawler/dashboard build` — succeeds.
4. Manual dev check at http://localhost:3000/entries:
   - Default (no filters, has data): rows render with comfortable spacing (~44-48px tall).
   - With `?q=football` and matches: matched substrings show with a faint coral tint + coral underline (no yellow).
   - When API returns no entries (empty database or no matching filter): centered Inbox or SearchX icon (size ~36, faded) + bold heading + small subcopy.
5. `grep -E "bg-yellow-200|dark:bg-yellow-800" apps/dashboard/components/entries/entries-table.tsx` returns nothing.
</verification>

<success_criteria>
- entries-table.tsx imports `{ Inbox, SearchX }` from lucide-react.
- The single `<mark>` element uses `bg-primary/10 rounded-sm px-0.5 underline decoration-primary decoration-2 underline-offset-2 text-inherit`.
- Empty state branch returns the centered icon + heading + subcopy pattern with the exact strings from UI-SPEC Copywriting Contract.
- Each of the four body `<TableCell>` elements has `py-3` (none uses `py-3.5`).
- All type-check, vitest, and Next.js build commands succeed.
</success_criteria>

<output>
After completion, create `.planning/phases/13-frontend-design-refresh/13-02-SUMMARY.md` documenting:
- Exact strings inserted for the new mark and empty state.
- Cells touched and their final classNames.
- Verification command outputs.
- Any deviations from the UI-SPEC patterns (should be none).
</output>
