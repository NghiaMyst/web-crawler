---
phase: 13
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - apps/dashboard/lib/badge-styles.ts
  - apps/dashboard/components/charts/VolumeChart.tsx
  - apps/dashboard/components/jobs/JobsTable.tsx
  - apps/dashboard/components/notifications/NotificationsTable.tsx
  - apps/dashboard/components/alerts/AlertsTable.tsx
  - apps/dashboard/components/sources/SourcesTable.tsx
  - apps/dashboard/components/sources/SourceModal.tsx
  - apps/dashboard/components/alerts/AlertRuleModal.tsx
autonomous: true
requirements:
  - DESIGN-02
  - DESIGN-03
  - DESIGN-04
user_setup: []
must_haves:
  truths:
    - "Charts page renders each chart (Entries over time, Per-source breakdown) inside its own card with border-border, bg-card, shadow-sm, rounded-lg, p-6"
    - "Chart series colors still resolve via var(--chart-1) through var(--chart-5) — coral remains series 1"
    - "Charts page heading uses text-foreground (not text-zinc-700)"
    - "All four management tables (Jobs, Sources, Alerts, Notifications) wrap their <Table> in `rounded-md border border-border bg-card overflow-x-auto` (replacing border-zinc-200 bg-white) and use bg-muted/50 header, text-muted-foreground head cells, hover:bg-muted/30 rows"
    - "Job retry button uses variant=\"default\" (coral primary fill) instead of variant=\"outline\""
    - "Status, channel, and alert condition badge style classes are exported from a single apps/dashboard/lib/badge-styles.ts file and the four tables import from it"
    - "Source and Alert Rule modals' section headers / footers / borders use bg-muted / border-border / text-muted-foreground / text-foreground tokens — no zinc-100, zinc-200, zinc-500, zinc-700, zinc-50/50 hardcodes remain in those two files"
    - "Modal primary action button labels are 'Save Source' / 'Save Rule' (not 'Save changes'); secondary button label is 'Discard Changes' (not 'Cancel')"
    - "Dashboard still builds with zero TypeScript errors"
  artifacts:
    - path: "apps/dashboard/lib/badge-styles.ts"
      provides: "Single source of truth for status/channel/condition badge className strings"
      exports:
        - "JOB_STATUS_STYLES"
        - "NOTIF_STATUS_STYLES"
        - "CHANNEL_STYLES"
        - "ALERT_CONDITION_STYLES"
    - path: "apps/dashboard/components/charts/VolumeChart.tsx"
      provides: "Card-wrapped chart sections + semantic-token heading"
      contains: "rounded-lg border border-border bg-card shadow-sm p-6"
    - path: "apps/dashboard/components/jobs/JobsTable.tsx"
      provides: "Semantic-token container + import from badge-styles + coral Retry button"
      contains: "import { JOB_STATUS_STYLES } from"
      contains_also: 'variant="default"'
    - path: "apps/dashboard/components/notifications/NotificationsTable.tsx"
      provides: "Semantic-token container + import from badge-styles"
      contains: "from '@/lib/badge-styles'"
    - path: "apps/dashboard/components/alerts/AlertsTable.tsx"
      provides: "Semantic-token container + import from badge-styles"
      contains: "from '@/lib/badge-styles'"
    - path: "apps/dashboard/components/sources/SourcesTable.tsx"
      provides: "Semantic-token container"
      contains: "border-border bg-card"
    - path: "apps/dashboard/components/sources/SourceModal.tsx"
      provides: "Semantic-token section headers + primary action 'Save Source' + secondary 'Discard Changes' / 'Close'"
      contains: "Save Source"
    - path: "apps/dashboard/components/alerts/AlertRuleModal.tsx"
      provides: "Semantic-token section headers + primary action 'Save Rule' + secondary 'Discard Changes' / 'Close'"
      contains: "Save Rule"
  key_links:
    - from: "apps/dashboard/components/jobs/JobsTable.tsx"
      to: "apps/dashboard/lib/badge-styles.ts"
      via: "import { JOB_STATUS_STYLES }"
      pattern: "import \\{ JOB_STATUS_STYLES \\} from '@/lib/badge-styles'"
    - from: "apps/dashboard/components/notifications/NotificationsTable.tsx"
      to: "apps/dashboard/lib/badge-styles.ts"
      via: "import { NOTIF_STATUS_STYLES, CHANNEL_STYLES }"
      pattern: "from '@/lib/badge-styles'"
    - from: "apps/dashboard/components/alerts/AlertsTable.tsx"
      to: "apps/dashboard/lib/badge-styles.ts"
      via: "import { ALERT_CONDITION_STYLES, CHANNEL_STYLES }"
      pattern: "from '@/lib/badge-styles'"
    - from: "apps/dashboard/components/charts/VolumeChart.tsx"
      to: "apps/dashboard/app/globals.css --chart-1..5"
      via: "CHART_COLORS array reads var(--chart-N) — unchanged"
      pattern: "var\\(--chart-1\\)"
---

<objective>
Apply the third bundle of Variant B polish to the non-entries surfaces of the dashboard:
- **D-07** wrap each VolumeChart section in a Shadcn-style card (`rounded-lg border border-border bg-card shadow-sm p-6`) and convert the chart heading + empty-state colors from `text-zinc-*` to `text-foreground` / `text-muted-foreground`. Do NOT change the chart series colors (already correct via `--chart-*`).
- **D-08** standardize the four management tables (Jobs, Sources, Alerts, Notifications) onto semantic CSS variables (`border-border bg-card`, `bg-muted/50` header, `text-muted-foreground` head text, `hover:bg-muted/30` rows) and lift the duplicated `STATUS_STYLES` / channel / condition objects into a single shared `lib/badge-styles.ts` module. Also flip the Jobs Retry button from `variant="outline"` to `variant="default"` per UI-SPEC Copywriting Contract (coral primary CTA).
- **D-09** update the Source and Alert Rule modal palettes: section-header backgrounds use `bg-muted` (not `bg-zinc-100`), borders use `border-border` (not `border-zinc-200`), labels use `text-foreground` / `text-muted-foreground`, primary action labels become "Save Source" / "Save Rule" (per UI-SPEC), secondary button label becomes "Discard Changes" or "Close" (per UI-SPEC — never "Cancel").

Purpose: Closes D-07/D-08/D-09 gaps from RESEARCH.md "What STILL NEEDS WORK" so the management surfaces match the formalized palette established in Plan 01. Lifting badge styles into a shared module removes the three current duplicated `STATUS_STYLES` definitions and prepares the codebase for future status additions.

Output: One new `lib/badge-styles.ts` and seven updated component files that compile cleanly and render the Variant B palette consistently across charts, management pages, and modals.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-frontend-design-refresh/13-CONTEXT.md
@.planning/phases/13-frontend-design-refresh/13-RESEARCH.md
@.planning/phases/13-frontend-design-refresh/13-UI-SPEC.md
@.planning/phases/13-frontend-design-refresh/13-01-SUMMARY.md

<interfaces>
<!-- Badge styles (UI-SPEC Status Badge Color System lines 175-211) -->

// apps/dashboard/lib/badge-styles.ts — NEW FILE
export const JOB_STATUS_STYLES: Record<string, string> = {
  pending: 'border-amber-500 text-amber-600 bg-amber-50',
  running: 'border-blue-500 text-blue-600 bg-blue-50',
  done:    'border-green-600 text-green-600 bg-green-50',
  failed:  'border-red-500 text-red-500 bg-red-50',
  skipped: 'border-zinc-400 text-zinc-500 bg-zinc-50',
};
export const NOTIF_STATUS_STYLES: Record<'sent' | 'failed', string> = {
  sent:   'border-green-600 text-green-600 bg-green-50',
  failed: 'border-red-500 text-red-500 bg-red-50',
};
export const CHANNEL_STYLES: Record<'telegram' | 'discord', string> = {
  telegram: 'border-blue-400 text-blue-500 bg-blue-50',
  discord:  'border-indigo-500 text-indigo-500 bg-indigo-50',
};
export const ALERT_CONDITION_STYLES: Record<'new_item' | 'field_changed' | 'threshold', string> = {
  new_item:      'border-emerald-500 text-emerald-600 bg-emerald-50',
  field_changed: 'border-sky-500 text-sky-600 bg-sky-50',
  threshold:     'border-orange-500 text-orange-600 bg-orange-50',
};

<!-- Current type definitions in @/types/api -->
type JobStatus = 'pending' | 'running' | 'done' | 'failed' | 'skipped';
type AlertCondition = { type: 'new_item' } | { type: 'field_changed'; fieldPath: string } | { type: 'threshold'; fieldPath: string; threshold: number };
type AlertChannel = 'telegram' | 'discord';

<!-- Shadcn Button variants used here: -->
// variant="default"     → bg-primary text-primary-foreground (coral)
// variant="outline"     → bordered, no fill — used for secondary modal action
// variant="ghost"       → no border, no fill — used for Edit / Delete icon buttons
// variant="destructive" → bg-destructive — used for Delete confirmation dialog only

<!-- Management table CURRENT zinc → semantic mapping (UI-SPEC Token Formalization Map):
  border-zinc-200 bg-white   → border-border bg-card
  bg-zinc-100                → bg-muted/50
  text-zinc-700              → text-muted-foreground
  hover:bg-zinc-50           → hover:bg-muted/30
  text-zinc-600 (body cells) → text-muted-foreground
-->

<!-- Modal CURRENT zinc → semantic mapping:
  border-zinc-100              (header/footer divider)  → border-border
  border-zinc-200 bg-zinc-50/50 (section card)          → border-border bg-muted/40
  bg-zinc-100                  (section header bg, in SourceModal/AlertRuleModal) → bg-muted
  text-zinc-500 / text-zinc-700 (labels, hints)         → text-muted-foreground
  text-zinc-900                (title)                  → text-foreground
  border-zinc-300 accent-zinc-900 (checkboxes)          → border-input accent-primary
  bg-zinc-50/50                (footer bg)              → bg-muted/40
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create shared lib/badge-styles.ts and wire into all four management tables (D-08)</name>
  <files>
    apps/dashboard/lib/badge-styles.ts,
    apps/dashboard/components/jobs/JobsTable.tsx,
    apps/dashboard/components/notifications/NotificationsTable.tsx,
    apps/dashboard/components/alerts/AlertsTable.tsx,
    apps/dashboard/components/sources/SourcesTable.tsx
  </files>
  <read_first>
    - apps/dashboard/components/jobs/JobsTable.tsx (full file — 116 lines; existing STATUS_STYLES on lines 11-17, container/header zinc classes on lines 41-50, hover on line 57, Retry Button on lines 92-105)
    - apps/dashboard/components/notifications/NotificationsTable.tsx (full file — 78 lines; container on line 21, header on line 23, inline ternary for status badge on lines 38-43, inline ternary for channel badge on lines 50-55)
    - apps/dashboard/components/alerts/AlertsTable.tsx (full file — 119 lines; existing conditionBadgeClass + channelBadgeClass on lines 14-29, container on line 43)
    - apps/dashboard/components/sources/SourcesTable.tsx (full file — 93 lines; container on line 23, header on line 25, hover on line 39, body cell text-zinc-500 on line 50)
    - apps/dashboard/types/api.ts (confirm JobStatus, AlertRule['channel'], AlertRule['condition']['type'] string unions for use in badge-styles.ts typing)
    - .planning/phases/13-frontend-design-refresh/13-UI-SPEC.md (Status Badge Color System lines 175-211; Management Page Tables lines 364-389)
  </read_first>
  <action>
    **Step A — Create `apps/dashboard/lib/badge-styles.ts` (NEW FILE).**
    Write exactly this content:
    ```typescript
    /**
     * Shared status / channel / condition badge className constants.
     * Used by JobsTable, NotificationsTable, AlertsTable to render
     * <Badge variant="outline" className={STYLES[key]}> with consistent
     * colors across the dashboard (D-08).
     */

    import type { JobStatus, AlertRule, NotificationLog } from '@/types/api';

    type Channel = AlertRule['channel'];
    type NotifStatus = NotificationLog['status'];
    type ConditionType = AlertRule['condition']['type'];

    export const JOB_STATUS_STYLES: Record<JobStatus, string> = {
      pending: 'border-amber-500 text-amber-600 bg-amber-50',
      running: 'border-blue-500 text-blue-600 bg-blue-50',
      done:    'border-green-600 text-green-600 bg-green-50',
      failed:  'border-red-500 text-red-500 bg-red-50',
      skipped: 'border-zinc-400 text-zinc-500 bg-zinc-50',
    };

    export const NOTIF_STATUS_STYLES: Record<NotifStatus, string> = {
      sent:   'border-green-600 text-green-600 bg-green-50',
      failed: 'border-red-500 text-red-500 bg-red-50',
    };

    export const CHANNEL_STYLES: Record<Channel, string> = {
      telegram: 'border-blue-400 text-blue-500 bg-blue-50',
      discord:  'border-indigo-500 text-indigo-500 bg-indigo-50',
    };

    export const ALERT_CONDITION_STYLES: Record<ConditionType, string> = {
      new_item:      'border-emerald-500 text-emerald-600 bg-emerald-50',
      field_changed: 'border-sky-500 text-sky-600 bg-sky-50',
      threshold:     'border-orange-500 text-orange-600 bg-orange-50',
    };

    /** Generic active/inactive on/off status badge (shared between Sources and Alerts isActive). */
    export const ACTIVE_INACTIVE_STYLES = {
      active:   'border-green-600 text-green-600 bg-green-50',
      inactive: 'border-red-500 text-red-500 bg-red-50',
    } as const;
    ```

    If `JobStatus` / `NotificationLog['status']` / `AlertRule['channel']` / `AlertRule['condition']['type']` are not exported with those exact names in `@/types/api`, use the names that exist (read types/api.ts first). Do NOT introduce new type names — alias the existing exports to the names above. Falls back to `Record<string, string>` only if the union cannot be derived — but first prefer the typed Record so misuses are caught at compile time.

    **Step B — Refactor `apps/dashboard/components/jobs/JobsTable.tsx`:**
    1. Delete the local `STATUS_STYLES` constant (lines 11-17 currently).
    2. Add an import after the existing `Button` import: `import { JOB_STATUS_STYLES } from '@/lib/badge-styles';`
    3. Update the `<Badge ... className={STATUS_STYLES[job.status]}>` line (around 62) to `className={JOB_STATUS_STYLES[job.status]}`.
    4. Container className (line 41): replace `"rounded-md border border-zinc-200 bg-white overflow-x-auto"` with `"rounded-md border border-border bg-card overflow-x-auto"`.
    5. TableHeader className (line 43): replace `"bg-zinc-100"` with `"bg-muted/50"`.
    6. Each TableHead (lines 45-50, six occurrences): replace `text-zinc-700` with `text-muted-foreground`.
    7. TableRow body row hover (line 57): replace `"hover:bg-zinc-50"` with `"hover:bg-muted/30"`.
    8. Body cell colors (lines 70, 76, 81-82, 84-85, 87-88): replace `text-zinc-600` with `text-muted-foreground` and `text-zinc-700` with `text-foreground` (URL/source cells keep the readability bump).
    9. Retry Button (lines 92-105): change `variant="outline"` to `variant="default"`. Add `size="sm"` retained as-is. The coral Retry CTA is the D-08 visual.

    **Step C — Refactor `apps/dashboard/components/notifications/NotificationsTable.tsx`:**
    1. Add import at top: `import { NOTIF_STATUS_STYLES, CHANNEL_STYLES } from '@/lib/badge-styles';`
    2. Replace the inline ternary for status badge className (lines 38-43) with `NOTIF_STATUS_STYLES[log.status]`.
    3. Replace the inline ternary for channel badge className (lines 50-55) with `CHANNEL_STYLES[log.channel]`.
    4. Container className (line 21): replace `"rounded-md border border-zinc-200 bg-white overflow-x-auto"` with `"rounded-md border border-border bg-card overflow-x-auto"`.
    5. TableHeader className (line 23): replace `"bg-zinc-100"` with `"bg-muted/50"`.
    6. Each TableHead (lines 25-29, five occurrences): replace `text-zinc-700` with `text-muted-foreground`.
    7. TableRow hover (line 34): replace `"hover:bg-zinc-50"` with `"hover:bg-muted/30"`.
    8. Body cells (lines 61, 64, 68): replace `text-zinc-500` and `text-zinc-600` with `text-muted-foreground`.

    **Step D — Refactor `apps/dashboard/components/alerts/AlertsTable.tsx`:**
    1. Delete the local `conditionBadgeClass` and `channelBadgeClass` helper functions (lines 14-29).
    2. Add import at top: `import { ALERT_CONDITION_STYLES, CHANNEL_STYLES, ACTIVE_INACTIVE_STYLES } from '@/lib/badge-styles';`
    3. Replace `conditionBadgeClass(r.condition.type)` call with `ALERT_CONDITION_STYLES[r.condition.type]`.
    4. Replace `channelBadgeClass(r.channel)` call with `CHANNEL_STYLES[r.channel]`.
    5. Replace inline isActive ternary (`r.isActive ? 'border-green-600 text-green-600' : 'border-red-500 text-red-500'`) with `r.isActive ? ACTIVE_INACTIVE_STYLES.active : ACTIVE_INACTIVE_STYLES.inactive`.
    6. Container className (line 43): replace `"rounded-md border border-zinc-200 bg-white overflow-x-auto"` with `"rounded-md border border-border bg-card overflow-x-auto"`.
    7. TableHeader className (line 45): replace `"bg-zinc-100"` with `"bg-muted/50"`.
    8. Each TableHead (lines 47-52, six occurrences): replace `text-zinc-700` with `text-muted-foreground`.
    9. TableRow conditional className (line 59): replace `'hover:bg-zinc-50'` with `'hover:bg-muted/30'`. Keep the `r._pending ? 'opacity-60'` branch.
    10. Loader2 className inside `_pending` (line 65): replace `text-zinc-500` with `text-muted-foreground`.

    **Step E — Refactor `apps/dashboard/components/sources/SourcesTable.tsx`:**
    1. Add import at top: `import { ACTIVE_INACTIVE_STYLES } from '@/lib/badge-styles';`
    2. Container className (line 23): replace `"rounded-md border border-zinc-200 bg-white overflow-x-auto"` with `"rounded-md border border-border bg-card overflow-x-auto"`.
    3. TableHeader (line 25): replace `"bg-zinc-100"` with `"bg-muted/50"`.
    4. Each TableHead (lines 27-32): replace `text-zinc-700` with `text-muted-foreground`.
    5. TableRow conditional (line 39): replace `'hover:bg-zinc-50'` with `'hover:bg-muted/30'`.
    6. Body cell on line 50: replace `text-zinc-500` with `text-muted-foreground`.
    7. Loader2 (line 45): replace `text-zinc-500` with `text-muted-foreground`.
    8. Active/Inactive Badge className (line 59): replace `s.isActive ? 'border-green-600 text-green-600' : 'border-red-500 text-red-500'` with `s.isActive ? ACTIVE_INACTIVE_STYLES.active : ACTIVE_INACTIVE_STYLES.inactive`.

    Do NOT change any other logic, props, imports order, JSX structure, or event handlers in these files. The visual identity stays exactly the same — only the underlying class strings get unified.
  </action>
  <verify>
    <automated>cd apps/dashboard && pnpm type-check && pnpm test && [ -f lib/badge-styles.ts ] && grep -q "JOB_STATUS_STYLES" lib/badge-styles.ts && grep -q "from '@/lib/badge-styles'" components/jobs/JobsTable.tsx && grep -q "from '@/lib/badge-styles'" components/notifications/NotificationsTable.tsx && grep -q "from '@/lib/badge-styles'" components/alerts/AlertsTable.tsx && grep -q "from '@/lib/badge-styles'" components/sources/SourcesTable.tsx && ! grep -rE "border-zinc-200 bg-white" components/jobs components/notifications components/alerts components/sources && ! grep -rE "bg-zinc-100" components/jobs components/notifications components/alerts components/sources && grep -q 'variant="default"' components/jobs/JobsTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/dashboard/lib/badge-styles.ts` exists and exports JOB_STATUS_STYLES, NOTIF_STATUS_STYLES, CHANNEL_STYLES, ALERT_CONDITION_STYLES, ACTIVE_INACTIVE_STYLES
    - Each of the four management table files imports from `@/lib/badge-styles`
    - `grep -E "border-zinc-200 bg-white"` against the four management-table files returns ZERO matches
    - `grep -E "bg-zinc-100"` against the four management-table files returns ZERO matches
    - `grep -E "text-zinc-700|hover:bg-zinc-50"` against the four management-table files returns ZERO matches (head text + row hover converted)
    - JobsTable Retry Button line contains `variant="default"` (was `variant="outline"`) — `grep -A2 'aria-label={`Retry job' apps/dashboard/components/jobs/JobsTable.tsx | grep -q 'variant="default"'` succeeds
    - JobsTable no longer contains a local `const STATUS_STYLES =`
    - AlertsTable no longer contains a local `function conditionBadgeClass` or `function channelBadgeClass`
    - `pnpm --filter @web-crawler/dashboard type-check` exits 0
    - `pnpm --filter @web-crawler/dashboard test` exits 0
  </acceptance_criteria>
  <done>The new lib/badge-styles.ts compiles, all four tables import from it, no zinc hardcodes remain in those four files, and the Jobs Retry button is the coral primary variant.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wrap VolumeChart sections in cards and convert zinc to semantic tokens (D-07)</name>
  <files>apps/dashboard/components/charts/VolumeChart.tsx</files>
  <read_first>
    - apps/dashboard/components/charts/VolumeChart.tsx (full file — 134 lines; CHART_COLORS lines 15-21 must stay untouched, isEmpty block lines 73-80, two `<section>` elements at lines 82 and 107)
    - .planning/phases/13-frontend-design-refresh/13-UI-SPEC.md (Chart Cards section lines 337-360; anti-pattern #2 — do not modify CHART_COLORS)
  </read_first>
  <action>
    Modify `apps/dashboard/components/charts/VolumeChart.tsx`:

    1. **Add lucide import** after the recharts imports (around line 7): `import { BarChart3 } from 'lucide-react';`

    2. **Empty-state block (lines 73-80):** replace
       ```tsx
       <div className="flex flex-col items-center justify-center text-center py-16 gap-4">
         <h2 className="text-xl font-semibold text-zinc-900">No data available</h2>
         <p className="text-sm text-zinc-600 max-w-md">
           Entry volume data will appear once crawl sources are active and entries are collected.
         </p>
       </div>
       ```
       with
       ```tsx
       <div className="flex flex-col items-center justify-center rounded-lg border border-border p-12 gap-3 text-muted-foreground">
         <BarChart3 size={36} className="opacity-40" aria-hidden="true" />
         <p className="text-sm font-medium text-foreground">No data to display</p>
         <p className="text-xs">Charts appear once entries have been crawled.</p>
       </div>
       ```
       This matches the UI-SPEC Empty State Copy row for Charts.

    3. **Wrap the first `<section>` (Entries over time, line 82) in a card.** Replace
       ```tsx
       <section className="space-y-2">
         <h2 className="text-sm font-semibold text-zinc-700">Entries over time</h2>
       ```
       with
       ```tsx
       <section className="rounded-lg border border-border bg-card shadow-sm p-6 space-y-3">
         <h2 className="text-sm font-semibold text-foreground">Entries over time</h2>
       ```

    4. **Wrap the second `<section>` (Per-source breakdown, line 107) in a card.** Replace
       ```tsx
       <section className="space-y-2">
         <h2 className="text-sm font-semibold text-zinc-700">Per-source breakdown</h2>
       ```
       with
       ```tsx
       <section className="rounded-lg border border-border bg-card shadow-sm p-6 space-y-3">
         <h2 className="text-sm font-semibold text-foreground">Per-source breakdown</h2>
       ```

    5. **Do NOT modify** `CHART_COLORS` (lines 15-21), `pivotVolumeData`, `getSourceNames`, `handleRangeChange`, the outer Select wrapping the range picker, or anything inside `<ResponsiveContainer>`. The chart series colors must keep resolving via `var(--chart-1)` through `var(--chart-5)`.
  </action>
  <verify>
    <automated>cd apps/dashboard && grep -q "import { BarChart3 } from 'lucide-react'" components/charts/VolumeChart.tsx && [ "$(grep -c 'rounded-lg border border-border bg-card shadow-sm p-6 space-y-3' components/charts/VolumeChart.tsx)" -ge 2 ] && grep -q "Charts appear once entries have been crawled" components/charts/VolumeChart.tsx && ! grep -q "text-zinc-700\|text-zinc-900\|text-zinc-600" components/charts/VolumeChart.tsx && grep -q "var(--chart-1)" components/charts/VolumeChart.tsx && grep -q "var(--chart-5)" components/charts/VolumeChart.tsx && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rounded-lg border border-border bg-card shadow-sm p-6 space-y-3"` against VolumeChart.tsx returns 2 (the two chart sections)
    - `grep -q "import { BarChart3 } from 'lucide-react'"` succeeds
    - `grep -q "No data to display"` succeeds and `grep -q "Charts appear once entries have been crawled"` succeeds (empty state)
    - `grep -E "text-zinc-(700|900|600)"` against VolumeChart.tsx returns ZERO matches
    - `grep -q "var(--chart-1)"` succeeds (CHART_COLORS array preserved)
    - `grep -q "var(--chart-5)"` succeeds (CHART_COLORS array preserved)
    - `grep -E "^const CHART_COLORS"` succeeds (array still declared)
    - `pnpm --filter @web-crawler/dashboard type-check` exits 0
    - `pnpm --filter @web-crawler/dashboard test` exits 0
  </acceptance_criteria>
  <done>Both chart sections are now in card containers with semantic colors, the empty state is the UI-SPEC pattern, and CHART_COLORS is untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Refactor SourceModal and AlertRuleModal palettes and button labels (D-09)</name>
  <files>
    apps/dashboard/components/sources/SourceModal.tsx,
    apps/dashboard/components/alerts/AlertRuleModal.tsx
  </files>
  <read_first>
    - apps/dashboard/components/sources/SourceModal.tsx (full file — 340 lines; DialogHeader on lines 139-149, three section cards on lines 155-289 each with `bg-zinc-50/50` + `border-zinc-200` and `bg-zinc-100`-styled section header strip — wait, they currently use `bg-zinc-50/50` outer + plain `px-4 py-3 border-b border-zinc-200` inner (section label background is inherited). Confirm by re-reading. Footer on line 298, Cancel button on line 299-307, primary action on line 308-310, Field helper at lines 318-339)
    - apps/dashboard/components/alerts/AlertRuleModal.tsx (full file — 385 lines; DialogHeader on lines 166-175, condition + delivery sections on lines 217-321 with the same `bg-zinc-50/50` + `border-zinc-200` outer pattern, footer on line 340, Cancel button on line 341-349, primary action on line 350-352, Field helper at lines 360-384)
    - .planning/phases/13-frontend-design-refresh/13-UI-SPEC.md (Modals section lines 392-422; Copywriting Contract lines 218-234 for button labels)
  </read_first>
  <action>
    **File A — `apps/dashboard/components/sources/SourceModal.tsx`:**

    1. DialogHeader (line 140): replace `"px-6 pt-6 pb-4 border-b border-zinc-100"` with `"px-6 pt-6 pb-4 border-b border-border"`.
    2. DialogTitle (line 141): replace `"text-base font-semibold text-zinc-900"` with `"text-base font-semibold text-foreground"`.
    3. DialogDescription (line 144): replace `"text-sm text-zinc-500 mt-0.5"` with `"text-sm text-muted-foreground mt-0.5"`.
    4. Each of the three section card outers (Identity line 155, Source line 180, Crawl settings line 227): replace `"rounded-lg border border-zinc-200 bg-zinc-50/50"` with `"rounded-lg border border-border bg-muted/40"`.
    5. Each of the three section header divs (lines 156, 181, 228): replace `"px-4 py-3 border-b border-zinc-200"` with `"px-4 py-3 border-b border-border"`.
    6. Each of the three section label `<p>` elements (lines 157, 182, 229): replace `"text-xs font-semibold uppercase tracking-wide text-zinc-500"` with `"text-xs font-semibold uppercase tracking-wide text-muted-foreground"`.
    7. Inline hint paragraphs (lines 167 + 284): replace `"text-xs text-zinc-400 mt-1"` with `"text-xs text-muted-foreground mt-1"`.
    8. Status section Label (line 272) and the checkbox text span (line 284) — replace `text-zinc-700` with `text-foreground` (on Label) and `text-zinc-700` with `text-foreground` (on the span).
    9. Status checkbox `<input>` className (line 282): replace `"h-4 w-4 rounded border-zinc-300 accent-zinc-900 cursor-pointer"` with `"h-4 w-4 rounded border-input accent-primary cursor-pointer"`.
    10. Server-error box (line 291): replace `"rounded-md bg-red-50 border border-red-200 px-3 py-2.5"` — keep `bg-red-50 border-red-200` because those are semantic error colors per UI-SPEC (they are already in the badge-styles equivalent palette; do not convert this one to muted).
    11. DialogFooter (line 298): replace `"px-6 py-4 border-t border-zinc-100 bg-zinc-50/50 flex justify-end gap-2"` with `"px-6 py-4 border-t border-border bg-muted/40 flex justify-end gap-2"`.
    12. Secondary button (line 299-307): change the label text from `Cancel` to a dynamic label — replace the button block with:
        ```tsx
        <Button
          type="button"
          variant="outline"
          onClick={() => onOpenChange(false)}
          disabled={isPending}
          className="text-muted-foreground"
        >
          {isDirty ? 'Discard Changes' : 'Close'}
        </Button>
        ```
        To get `isDirty`, extend the `useForm` destructure (around line 72-75) to include `formState: { errors, isDirty }`. The `isDirty` flag is provided by react-hook-form (already a dependency).
    13. Primary button (lines 308-310): replace the label text inside the button — replace `{isPending ? 'Saving…' : isEdit ? 'Save changes' : 'Create source'}` with `{isPending ? 'Saving…' : 'Save Source'}`. The label is constant ("Save Source") for both create and edit per UI-SPEC Copywriting Contract.
    14. Field helper (lines 318-339): inside `<Label>` at line 331, replace `text-zinc-700` with `text-foreground`.

    **File B — `apps/dashboard/components/alerts/AlertRuleModal.tsx`:**

    1. DialogHeader (line 166): replace `"px-6 pt-6 pb-4 border-b border-zinc-100"` with `"px-6 pt-6 pb-4 border-b border-border"`.
    2. DialogTitle (line 167): replace `text-zinc-900` with `text-foreground`.
    3. DialogDescription (line 170): replace `text-zinc-500` with `text-muted-foreground`.
    4. Condition section outer (line 217): replace `"rounded-lg border border-zinc-200 bg-zinc-50/50"` with `"rounded-lg border border-border bg-muted/40"`.
    5. Condition header div (line 218): replace `"px-4 py-3 border-b border-zinc-200"` with `"px-4 py-3 border-b border-border"`.
    6. Condition label `<p>` (line 219): replace `text-zinc-500` with `text-muted-foreground`.
    7. Hint paragraphs (lines 234, 249): replace `text-zinc-400` with `text-muted-foreground`.
    8. Delivery section outer (line 271): replace `"rounded-lg border border-zinc-200 bg-zinc-50/50"` with `"rounded-lg border border-border bg-muted/40"`.
    9. Delivery header div (line 272): replace `border-zinc-200` with `border-border`.
    10. Delivery label `<p>` (line 273): replace `text-zinc-500` with `text-muted-foreground`.
    11. Status section Label (line 297): replace `text-zinc-700` with `text-foreground`.
    12. Status checkbox `<input>` (line 305): replace `"peer h-4 w-4 rounded border-zinc-300 text-zinc-900 accent-zinc-900 cursor-pointer"` with `"peer h-4 w-4 rounded border-input text-primary accent-primary cursor-pointer"`.
    13. Status checkbox span (line 314): replace `text-zinc-700` with `text-foreground`.
    14. Source-select empty placeholder span (line 196): replace `text-zinc-400` with `text-muted-foreground`.
    15. Server-error box: keep `bg-red-50 border-red-200` (semantic error palette, same rationale as SourceModal).
    16. DialogFooter (line 340): replace `"px-6 py-4 border-t border-zinc-100 bg-zinc-50/50 flex justify-end gap-2"` with `"px-6 py-4 border-t border-border bg-muted/40 flex justify-end gap-2"`.
    17. Secondary button (line 341-349): change to dynamic label, mirroring SourceModal:
        ```tsx
        <Button
          type="button"
          variant="outline"
          onClick={() => onOpenChange(false)}
          disabled={isPending}
          className="text-muted-foreground"
        >
          {isDirty ? 'Discard Changes' : 'Close'}
        </Button>
        ```
        Add `isDirty` to the `useForm` destructure around line 84.
    18. Primary button (lines 350-352): replace `{isPending ? 'Saving…' : isEdit ? 'Save changes' : 'Create rule'}` with `{isPending ? 'Saving…' : 'Save Rule'}`.
    19. Field helper (lines 360-384): inside `<Label>` at line 375, replace `text-zinc-700` with `text-foreground`. The hint span (line 378): replace `text-zinc-400` with `text-muted-foreground`.

    Do NOT change any form schemas, server-action wiring, validation flow, useForm options, `register()` keys, or Select/SelectItem markup. The form must still submit and validate identically.

    Per UI-SPEC anti-pattern #9: "Save" / "Cancel" are not used as bare labels. The new labels are "Save Source" / "Save Rule" (primary) and "Discard Changes" / "Close" (secondary).
  </action>
  <verify>
    <automated>cd apps/dashboard && ! grep -rE "border-zinc-(100|200|300)|bg-zinc-(50/50|100)|text-zinc-(400|500|700|900)|accent-zinc-900" components/sources/SourceModal.tsx components/alerts/AlertRuleModal.tsx && grep -q "Save Source" components/sources/SourceModal.tsx && grep -q "Save Rule" components/alerts/AlertRuleModal.tsx && grep -q "Discard Changes" components/sources/SourceModal.tsx && grep -q "Discard Changes" components/alerts/AlertRuleModal.tsx && grep -q "isDirty" components/sources/SourceModal.tsx && grep -q "isDirty" components/alerts/AlertRuleModal.tsx && ! grep -q ">Cancel<" components/sources/SourceModal.tsx && ! grep -q ">Cancel<" components/alerts/AlertRuleModal.tsx && pnpm type-check && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "border-zinc-(100|200|300)|bg-zinc-(50/50|100)|text-zinc-(400|500|700|900)|accent-zinc-900" apps/dashboard/components/sources/SourceModal.tsx apps/dashboard/components/alerts/AlertRuleModal.tsx` returns ZERO matches
    - `grep -q "Save Source"` against SourceModal.tsx succeeds
    - `grep -q "Save Rule"` against AlertRuleModal.tsx succeeds
    - `grep -q "Save changes"` against both modal files returns ZERO matches
    - `grep -q "Create source"` and `grep -q "Create rule"` against the modal files return ZERO matches
    - `grep -q "Discard Changes"` succeeds in BOTH modal files
    - `grep -q ">Cancel<"` returns ZERO matches in both modal files
    - `grep -q "isDirty"` succeeds in BOTH modal files (signal that useForm destructure was extended)
    - `grep -c "border-border"` against SourceModal.tsx returns 5 or more (header divider, footer divider, 3 section outers + 3 section header dividers)
    - `grep -c "bg-muted/40"` against SourceModal.tsx returns 4 or more (3 section outers + footer)
    - Existing schemas (`sourceFormSchema`, `alertRuleFormSchema`) and DEFAULTS objects are unchanged
    - `pnpm --filter @web-crawler/dashboard type-check` exits 0
    - `pnpm --filter @web-crawler/dashboard test` exits 0 (existing source/alert modal-action tests in __tests__/ must continue to pass — they test the server actions, not the button labels)
  </acceptance_criteria>
  <done>Both modals render with the new palette, the new primary labels (Save Source / Save Rule) and dynamic secondary labels (Discard Changes / Close), and all schemas + actions still pass type-check and existing vitest specs.</done>
</task>

</tasks>

<threat_model>
N/A — purely visual/CSS/TSX changes, no new data flows, auth, or external integrations.
</threat_model>

<verification>
1. `pnpm --filter @web-crawler/dashboard type-check` — passes with zero TS errors.
2. `pnpm --filter @web-crawler/dashboard test` — all existing 8 vitest files pass.
3. `pnpm --filter @web-crawler/dashboard build` — Next.js build succeeds.
4. Manual dev check at http://localhost:3000:
   - `/charts`: two chart cards with subtle border + shadow; "Entries over time" and "Per-source breakdown" headings render in the foreground color (close-to-black on cream); chart lines/bars use coral first, then teal/purple/green/pink; empty state shows BarChart3 icon.
   - `/jobs`: table container has a soft cream-ish bg-card with `--border` divider; header row uses muted bg; failed-job Retry button is coral filled (not outlined).
   - `/sources`, `/alerts`, `/notifications`: same management-table treatment, badges use the consistent palette from `lib/badge-styles.ts`.
   - Open Add Source modal: section cards use muted bg with border-border outlines; labels read crisp foreground; primary button says "Save Source"; secondary button says "Close" until you edit a field then it says "Discard Changes".
   - Open Add Alert Rule modal: same palette; primary "Save Rule"; secondary "Close" → "Discard Changes" once dirty.
5. `grep -rE "border-zinc-200 bg-white|bg-zinc-100" apps/dashboard/components/{jobs,notifications,alerts,sources}` returns nothing. `grep -rE "text-zinc-700|hover:bg-zinc-50" apps/dashboard/components/{jobs,notifications,alerts,sources}` returns nothing.
</verification>

<success_criteria>
- `lib/badge-styles.ts` exists and exports four typed records (JOB_STATUS_STYLES, NOTIF_STATUS_STYLES, CHANNEL_STYLES, ALERT_CONDITION_STYLES) plus ACTIVE_INACTIVE_STYLES.
- Four management tables import from `@/lib/badge-styles` and contain no `bg-zinc-100` / `border-zinc-200 bg-white` / `hover:bg-zinc-50` / `text-zinc-700`.
- JobsTable Retry button is `variant="default"` (coral primary).
- VolumeChart wraps both chart sections in `rounded-lg border border-border bg-card shadow-sm p-6` cards, uses BarChart3 empty state, and preserves CHART_COLORS untouched.
- SourceModal + AlertRuleModal use only semantic CSS-variable classes (no zinc-* anywhere), have "Save Source" / "Save Rule" primary labels, and have dynamic "Discard Changes" / "Close" secondary labels via react-hook-form `isDirty`.
- type-check, test, and build commands all succeed.
</success_criteria>

<output>
After completion, create `.planning/phases/13-frontend-design-refresh/13-03-SUMMARY.md` documenting:
- The new lib/badge-styles.ts contents and exports.
- Per-file diff summary (which zinc strings became which semantic tokens).
- The Retry-button variant change and the modal-label changes.
- Verification command outputs.
- Confirmation that CHART_COLORS was not modified.
</output>
