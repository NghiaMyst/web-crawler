---
phase: 08-next-js-dashboard-alerts-charts
plan: 03
type: execute
wave: 3
depends_on: [08-04]
files_modified:
  - apps/dashboard/app/charts/page.tsx
  - apps/dashboard/components/charts/VolumeChart.tsx
  - apps/dashboard/components/layout/NavLinks.tsx
  - apps/dashboard/components/ui/chart.tsx
autonomous: true
requirements: [DASH-02]
tags: [nextjs, recharts, shadcn-charts, line-chart, bar-chart, date-range]

must_haves:
  truths:
    - "User can navigate to /charts and see entry volume line chart"
    - "Date range selector (7d/30d/90d) triggers refetch and re-renders chart"
    - "Per-source bar chart shown below line chart"
    - "Each source gets its own Line/Bar with ChartConfig color token"
    - "Empty state renders when no data is returned"
    - "Charts page passes Next.js build with zero TypeScript errors"
---

<objective>
Build the charts page at /charts (DASH-02). Shadcn Charts (Recharts wrapper) showing entry volume over time as a line chart and per-source breakdown as a stacked bar chart. Date range selector (7d/30d/90d) drives client-side refetch. Install shadcn chart component via CLI first.
</objective>
